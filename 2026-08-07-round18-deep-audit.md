# Kairos 文档库全面深度审计报告（第十八轮）

> **审计对象**：`D:\projects\kairos-memory` — Kairos「AI 记忆系统」文档设计仓库（无运行代码，168 项能力架构就绪，核心引擎未启动）  
> **审计基线**：changelog **0.0.43**（2026-08-07）；工作区含 0.0.42 / 0.0.43 两批次**未提交**改动（59 个变更文件，最后提交为 `f45d979` 0.0.41）  
> **审计范围**：`docs/` 全库权威文档 53 md + 3 yaml + 1 sql + 1 html + 1 json；`docs/analysis/`（131 份分析产物）仅作边界核查，不作内容审计  
> **审计日期**：2026-08-07  
> **本轮性质**：仅只读分析与核验，**未修改任何文档、未产生文件变更**（除本报告外）

---

## 模块一 · 审计概览

### 1.1 整体评价

Kairos 文档库的**设计事实层已达开发就绪**，但**治理执行层在最近两个批次出现质量回落**。

支撑「设计事实层就绪」的证据（本轮全部重新执行，非引用历史结论）：

- **门禁全绿**：`scripts/doc-audit.py` 18 类 + 14a + 6.8a/6.12a/6.13~6.19 全部通过，退出码 0。链接 2767 条、章节引用 2098 处、配置参数索引 370/370、债务闭环 13/13、元数据日期 183 份、锚点 0 断链、行尾 0 份 CRLF、废弃术语 0 处、决策编号未标注 0 处。`scripts/deep-audit.py` 无阻断项。
- **核心口径逐项重算属实**：168 功能（43 核心 + 125 扩展，144 为 Phase 0~2 小计已显式注明）、57 物理表、370 参数（224+146）、69 术语、38 错误码、66 操作、12 ADR、70 组件、88 端点（85 `/v1` + 3 非前缀）。
- **检索权重口径单一且无矛盾**：三信号（语义 0.50 / BM25 0.35 / 实体 0.15，候选域内融合）与四链路（语义 0.50 / 共现 0.20 / kNN 0.10 / 因果 0.20，候选扩展）分属管线两阶段，架构 §7.3a 有显式管线关系声明与术语消歧，configuration 双套配置项独立承载。历史 R14-03 确已闭环。
- **AP 机制落点抽验准确**：架构 0.0.41 版本记录声称的章节归属经逐行反查属实（AP-05 候选集集中度 → §3.9 ✔、AP-19 升华整合门控 → §5.2 ✔、AP-10 处理完降温 → §5.5 ✔、AP-09 间隔重复复习 → §9.2 ✔）。

支撑「治理执行层回落」的证据：

- 上一轮审计报告（F1–F9）的 0.0.43 修复批次，**9 项中仅 3 项实质闭环**，另有 **2 项误诊**、**2 项形式闭环内容空转**、**2 项修复过程新引入违规**。
- **0.0.43 批次自身新增 5 处零版本标记违规、4 处硬行号引用、1 处语义自指错误**——即「修复批次引入的缺陷数 ≈ 修复的缺陷数」。
- 门禁未能拦截上述任何一类，说明**自动化门禁与治理规则之间存在可枚举的覆盖缺口**（详见 R18-12 / R18-13）。

### 1.2 成熟度结论

**成熟度 = 放行（准开发就绪），但附条件。风险分布：0【高】 / 9【中】 / 6【低】。**

无【高】阻断项——设计事实正确性、跨文档一致性、核心口径均达标，开发可以启动。

**但建议在代码启动前优先闭环三项**，它们直接影响开发者的第一手理解与工具链可用性：

| 优先级 | 编号              | 一句话                                             |
| :-- | :-------------- | :---------------------------------------------- |
| P0  | R18-06 / R18-07 | OpenAPI 契约结构无效且无归属，无法用于 codegen/mock/校验，未达其立项目的 |
| P0  | R18-01 / R18-09 | README（全库索引）自相矛盾、计数过时、未索引两个核心资产                 |
| P1  | R18-02 / R18-03 | AP-01~28 编号双重归属 + 声明追踪链空转，声明事实源不可用              |

**流程层结论（比单点问题更重要）**：本轮最值得关注的不是任何单一缺陷，而是「审计→修复」闭环的质量控制缺失——修复批次未做回归自检，且**误诊型修复（R18-15）比未修复更有害**，因为它删除了正确的权威指针。建议将「修复批次自查清单」升级为强制门禁项（详见模块四·G）。

---

## 模块二 · 上一轮遗留项核销（F1–F9 逐项实测）

> 依项目「遗留项核销」惯例：上轮报告每项逐一实测，声明属实与否，不采信 changelog 自述。

| 上轮编号                         | 声称处置                             | 实测结论                                                                                                                                                                              | 判定                            |
| :--------------------------- | :------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------- |
| **F1 / F10** 零版本标记           | 移除正文 23 处 changelog 批次标记         | 架构 23 处确已清除 ✔；但 `data-model.md` L81 表格注释残留 `（外部理念吸收，changelog 0.0.39）` 未清；且本批次**新引入** 5 处同类违规                                                                                     | ⚠ **净未闭环**                    |
| **F2** MCP 工具数口径             | 「12 规范操作集」统一为「15 个 MCP Tool」     | **误诊**。「12 规范操作集」是架构 §7.3.1 定义的独立概念（CRUD+merge/fork/archive/restore/purge/link/unlink），与「15 个 MCP Tool」分属两层，原文无矛盾。改写后 L95 变为「将 15 个 MCP Tool…映射为 MCP Tool」的语义自指，且删除了原 §7.3.1 权威指针 | ✘ **误诊 + 回归**                 |
| **F3** AP 声明补登               | claim-matrix 补 AP 系列追踪节          | 表格已建 ✔；但 8 行「认知层声明」列内容完全相同且无实质内容，均指向 cognitive-foundation 版本记录，而该记录仅列编号不含定义；同时 AP 编号在架构版本记录中被赋予机制语义，形成双重归属                                                                        | ⚠ **形式闭环，实质空转**               |
| **F4** 参数待登记追缴               | 补登 D-422~D-425                   | 4 条五段式条目确已建立 ✔，追缴门禁落实                                                                                                                                                             | ✔ **属实**（但引入 4 处硬行号，见 R18-13） |
| **F5** 机器可读契约                | 新增 openapi.yaml + mcp-tools.json | 文件存在，81 路径 / 88 操作与端点口径一致 ✔；但 25 处路径参数未声明（OpenAPI 3.1 结构违规）、55 个写操作无 requestBody、88 个操作无 security、securitySchemes 定义后从未引用、tags 为空                                                 | ⚠ **交付物存在，未达目的**              |
| **F6** technology-stack 版本记录 | 补 0.0.39~0.0.43、updated 同步       | 版本记录已补至 0.0.43，frontmatter `updated: 2026-08-07` ✔                                                                                                                                | ✔ **属实**                      |
| **F7** data-model DDL 说明     | 补 DDL 承载说明                       | L29 已补，指向 schema-slice.sql ✔                                                                                                                                                      | ✔ **属实**（注记本身带批次标记，见 R18-12）  |
| **F8** analysis 目录边界         | README 补目录边界说明                   | 新增**两处**说明（L21、L126），两者在「是否计入 184 md」「是否随仓库分发」上互相矛盾，且 L21 沿用过时的「核心文档 55 份」                                                                                                        | ✘ **未闭环（反而制造矛盾）**             |
| **F9** 架构单文件超限               | 复核无动作                            | 实测 3953 行（上轮记 3952），锚点 0 断链 ✔，维持豁免合理                                                                                                                                              | ✔ **属实**                      |

**核销小结：9 项中 3 项属实闭环、2 项形式闭环、2 项误诊/未闭环、2 项修复引入新缺陷。**

---

## 模块三 · 问题清单（按 5 个维度分类）

### 维度 1 · 完整性与一致性

**R18-01【中】`docs/README.md` 目录边界说明自相矛盾且计数过时**

同一文档内三处口径互斥（README 是全库索引与计数权威）：

| 位置        | 是否计入 184 md                       | 核心文档数    | 是否随仓库分发   |
| :-------- | :-------------------------------- | :------- | :-------- |
| L21       | **不计入**                           | **55 份** | 不随仓库强制分发  |
| L126      | **计入**（含于总计 184 份）                | 56 份     | **随仓库分发** |
| L138（计数式） | 计入（184 = 53 核心 md + 131 analysis） | 56 份     | —         |

实测事实：`docs/` 下 md 共 184 份 = 核心 53 + analysis 131；yaml 3 份。故 **L138、L126 正确，L21 三项表述全错**。L21 的「55 份」是 0.0.43 之前的旧值（本批次已改为 56 却漏改此处）。门禁 [6] 数值检查只比对 L138，未覆盖 L21。

**R18-02【中】AP-01~28 编号双重归属，且权威定义源位于非权威目录**

- `claim-implementation-matrix.md` 声明 AP-01/02/03/05/06/19/22/28 为**认知基础层声明**，承载文档为 `cognitive-foundation.md`。
- `architecture-v0.1.0.md` 版本记录（L3951）将**同样这 8 个编号**赋予架构机制并给出具体章节：AP-01 查询期重建产物治理（§5.5）、AP-02 显式时效字段（§7.3a）、AP-03 遗忘决策懒求值（§5.2）、AP-05 候选集集中度信号（§3.9）、AP-06 任务内跨轮状态传递（§3.1）、AP-19 升华整合门控（§5.2）、AP-22 可及性轴实证注记（§7.3a）、AP-28 增量存储模式（§5.2）。
- 即 changelog 所述「认知基础 8 条声明 + 架构 22 条机制」的两个集合在编号上**完全重叠 8 个**，同一编号指向两个不同对象。
- 更根本的问题：AP-01~28 的**唯一实质定义源是 `docs/analysis/external-videos/absorption-proposals.md`**，而 README L126 明确声明 analysis 目录「不参与架构/规格权威口径」。权威文档引用了非权威目录中的编号定义，违反 §6 单一事实源。
- 附带缺口：`documentation-governance.md` §5 编号注册表（规定「新增命名空间必须先注册再使用」）**未登记 AP- 前缀**；门禁 [14] 编号连续性也未覆盖 AP 命名空间——实测 AP-08 在全部权威文档中出现 0 次，AP-07/14/15/17/24 各仅在 changelog 出现 1 次，断号无人发现。

**R18-03【中】claim-matrix AP 追踪节内容空转，声明事实源形成死链**

新增的 8 行 AP 表格，「认知层声明」列内容完全相同：`认知层声明（详见 cognitive-foundation.md 0.0.41 版本记录）`。而 `cognitive-foundation.md` 全文仅在 L1176（自身版本记录）出现一次 AP 编号，内容为「AP-01/02/03/05/06/19/22/28 认知层声明落地」——同样只有编号、没有定义。

追踪链：claim-matrix → cognitive-foundation 版本记录 → 仅编号列表 → **断**。声明矩阵作为「§6 单一事实源」的职责未实现，读者无法从任何权威文档得知这 8 条声明的内容是什么。

**R18-04【低】架构 0.0.41 版本记录行批次号自相矛盾**

`architecture-v0.1.0.md` L3951：版本列为 `0.0.41`，说明文字却写「外部理念吸收批次（**changelog 0.0.40**，22 项借鉴落地…）」。changelog 0.0.40 是「外部视频分析批次」，0.0.41 才是「外部理念吸收落地批次」。上轮 F1 识别出同类批次号错标并清理了正文 23 处，但版本记录行（属豁免范围保留）中的错标未一并更正。

**R18-05【低】「三链路融合与检索扩展」小节名与其四链路内容不符**

架构 L2425 小节标题为「三链路融合与检索扩展」，但其内容（L2421）定义的默认权重是**四链路**（语义 0.20+ 实体共现 0.20 + kNN 0.10 + 因果 0.20 → 0.50/0.20/0.10/0.20）。L3037 的术语消歧段亦仍将「三链路」定义为「语义/共现/kNN」三元组。0.0.34 已将四链路确立为唯一默认，但承载它的小节名与术语定义仍停留在三链路语义。

### 维度 2 · 缺失与过时

**R18-06【中】OpenAPI 契约结构无效，无法达成其立项目的**

程序化校验（PyYAML 解析 + 逐操作检查）结果：

| 检查项                | 结果                                  | 影响                                                        |
| :----------------- | :---------------------------------- | :-------------------------------------------------------- |
| paths / operations | 81 / 88 ✔                           | 与 88 端点口径一致，路径提取准确                                        |
| 路径参数声明             | **22 条模板路径、25 个 `{param}` 全部未声明**   | **违反 OpenAPI 3.1 规范**，spectral/openapi-generator 等工具会直接报错 |
| requestBody        | **55 个 POST/PUT/PATCH 操作全部缺失**      | 写接口无请求体定义，无法生成客户端/Mock                                    |
| security           | 全局与操作级**均未声明**（88/88）               | 契约中所有端点表现为免鉴权，与三级 API Key 设计冲突                            |
| securitySchemes    | 已定义 `apiKey`/`adminKey` 但**从未被引用**  | 悬空组件                                                      |
| tags               | 顶层 `tags: []` 为空，但操作引用了 `audit` 等标签 | 引用未声明标签                                                   |

F5 的立项理由是「自动生成客户端 SDK 骨架、请求/响应校验器、Mock Server」。当前文件因缺失路径参数而**无法通过规范校验**，上述用途一个都不成立。它目前的实际价值仅等同于一份端点清单。

对照：`mcp-tools.json` 15 个工具名与架构 §7.1a 清单**逐一吻合**（基础 12 + 关系管理 3）✔，但 15 个工具的 `inputSchema` 全部无 `properties`。

**R18-07【中】契约骨架的「Phase 0 补全」承诺无追缴条目、未进项目计划**

`openapi.yaml` 头部写明「须在 Phase 0 补全」「切勿将其当作已完成契约」。这是典型的「后续/未来版本」承诺，按 AGENTS.md §2.3 追缴门禁必须同步创建 `debt-collection.md` 条目（到期日 / 改动量 / 不做的后果 / 状态）。

实测：`debt-collection.md` 中检索 `openapi` / `OpenAPI` / `契约骨架` / `mcp-tools` **全部零命中**（当前 91 条 `### D-` 条目）；`project-plan.md` Phase 0（W1 项目骨架 / W2 数据库与 CI）交付物中**亦无契约补全项**，全文零命中「契约 / 机器可读 / openapi」。

结果：一个自称未完成的交付物，既无到期日也无责任归属，同时违反「追缴门禁」与「写了=做了」两条项目红线。讽刺之处在于——同一批次为落实追缴门禁而补登了 D-422~D-425。

**R18-08【低】`.gitignore` 未覆盖根目录审计报告，与「审计材料不随仓库分发」策略存在落地缺口**

0.0.30 确立「审计过程材料不随仓库分发」，reviews/ 目录已删除。但审计报告改放工作区根目录后，`.gitignore` 未添加对应规则（现有规则仅覆盖 `.claude/`、`scripts/_deep_audit_out.json`、`video-work/`、`outputs/`）。当前 `2026-08-07-document-audit-report.md` 以 untracked 状态存在，一次 `git add -A` 即会违反该策略。

### 维度 3 · 结构与组织

**R18-09【中】README 全库索引未收录两个核心资产，第三个仅出现在计数括号内**

程序化比对（核心目录全部 `.md/.yaml/.json/.sql/.html` 资产 vs README 全文）：

| 资产                                          | README 状态       | 问题                                          |
| :------------------------------------------ | :-------------- | :------------------------------------------ |
| `specification/schema-slice.sql`            | **完全未提及**       | 它是 data-model L29 指定的**全库 DDL 唯一承载**，却不在索引中 |
| `specification/api-contract/mcp-tools.json` | **完全未提及**       | 15 工具契约不可从索引发现                              |
| `specification/api-contract/openapi.yaml`   | 仅出现在 L138 计数括号内 | 无索引条目，规格文档表中无行                              |

AGENTS.md §2.2 规定「`docs/README.md` 是全库索引，改动文档结构必须同步它」——0.0.43 新建了目录与文件，只同步了计数、未同步索引。

**R18-10【低】`specification/api-contract/` 新目录未在目录结构规范中登记**

`AGENTS.md` §2.2 的目录树与 §4.1「文档体系职责」十目录划分均未包含 api-contract/；README 的「规格文档」表亦无该子目录说明。新增结构层级未纳入结构性文档，属结构变更连锁复核（治理 §2.2）漏项。

**R18-11【低】架构单文件 3953 行持续增长，导航维护成本递增**

治理 §4.2 对存量超限文档已豁免追溯拆分，本轮维持该判断。但需记录趋势：0.0.41→0.0.43 三个批次内该文件仍在增长，且它同时承载 22 条 AP 机制、12 规范操作集、六层栈全量规格。治理 §4.2 明确「近轮仍活跃增长的超限文档须在变更时评估拆分或补导航」——建议下批次执行一次显式评估并记录结论，而非默认维持。

### 维度 4 · 格式规范

**R18-12【中】§6 零版本标记纪律违规 6 处，其中 5 处由 0.0.43 修复批次自身引入**

治理 §6 豁免 1 原文：「豁免范围仅限债务账目字段；**正文段落、表格注释、yaml 注释等其他位置的批次标记一律禁止**」。实测违规：

| 位置                                    | 内容                                                      | 来源                       |
| :------------------------------------ | :------------------------------------------------------ | :----------------------- |
| `README.md` L21                       | `**分析产物目录说明（审计报告 F8，changelog 0.0.43）**`                | 0.0.43 新引入               |
| `README.md` L126                      | `**目录边界说明（审计报告 F8，changelog 0.0.43）**`                  | 0.0.43 新引入               |
| `claim-implementation-matrix.md` L107 | `（审计报告 F3，changelog 0.0.43）`                            | 0.0.43 新引入               |
| `data-model.md` L29                   | `**DDL 承载说明（审计报告 F7，changelog 0.0.43）**`                | 0.0.43 新引入               |
| `api-contract/openapi.yaml` 头部        | `# AUTO-GENERATED from ... (changelog 0.0.43, 审计报告 F5)` | 0.0.43 新引入（yaml 注释，明文禁止） |
| `data-model.md` L81                   | `**conditions 子结构约定（外部理念吸收，changelog 0.0.39）**`         | F1 清扫遗漏                  |

补充观察（不计入违规数）：`integration-design.md` L93 的 `> **外部理念吸收配套**（changelog 0.0.39）` 与 `engineering-workflow.md` L68 的 `（0.0.22 批次…处置记录见 changelog 0.0.22）` 同属正文批次标记，建议一并纳入整改评估；`documentation-governance.md` L106 执行记录段的批次列举属审计执行日志，性质接近版本记录，本轮不判违规但建议在 §6 中明确其豁免地位。

**R18-13【中】D-422~D-425 引入 4 处硬行号引用，门禁 6.15 存在可枚举漏检**

0.0.28 已「整体废除硬行号引用」（configuration 附录 A 136 处改语义引用），门禁 6.15 承担守卫。但 0.0.43 新增的四条债务条目「工程简化」字段写入：

- D-422：`架构 L1496 引用「集中度超阈，阈值参数待登记」`
- D-423：`架构 L1993 引用…`
- D-424：`架构 L2472 引用…`
- D-425：`架构 L3388 引用…`

门禁仍报「[6.15] 硬行号引用禁令: 0 处残留」。根因已定位到具体代码——`scripts/doc-audit.py:571` 正则为 `[\w.\-/]+\.md:\d+(?:-\d+)?`，**只匹配 `文件名.md:行号` 形态**，对「中文文档名 + 空格 + `L` + 数字」形态完全不覆盖。

风险实证：这四个行号当前确实准确（已逐一反查命中对应机制文本），但架构文件在最近三个批次持续变动——这正是 0.0.28 废除硬行号的原因。

**R18-14【低】D-422~D-425 标题携带批次标记，超出豁免 1 的「账目字段」边界**

四条债务的三级标题形如 `### D-422 候选集集中度阈值（参数待登记，0.0.43 追缴）`。豁免 1 授权范围是「状态」「历史背景」「来源」等**账目字段**，标题不在其列。建议将批次信息下沉至「历史背景」字段（该字段已含同类信息），标题保持纯语义。

### 维度 5 · 核心文档准确性

**结论：认知基础、架构设计、技术选型三份核心文档的设计事实层准确性通过验证**，未发现事实性错误、口径矛盾或层次混淆。具体验证见 1.1 节。仅发现一处由上轮误诊修复引入的表述缺陷：

**R18-15【中】`technology-stack.md` §七 因 F2 误诊修复产生语义自指并丢失权威指针**

git diff 实测：

```
- 将 12 规范操作集（[架构 §7.3.1](...)）映射为 MCP Tool
+ 将 15 个 MCP Tool（基础工具集 12…另加关系管理 3 工具；构成口径见 [架构 §7.1a](...)）映射为 MCP Tool
```

两处问题：

1. **语义自指**——「将 15 个 MCP Tool …… 映射为 MCP Tool」，主语与宾语同一，句子不再表达任何映射关系。原句「将 12 规范操作集映射为 MCP Tool」语义完整且正确。
2. **权威指针丢失**——原文指向 §7.3.1（12 规范操作集的定义章节，架构中确实存在，L2922），改后指向 §7.1a（MCP Bridge 实现）。技术栈文档从此不再引用规范操作集的权威定义。

补充说明：「12 规范操作集」这一术语在 `architecture §7.3.1`、`feature-list A-26`、`configuration KAIROS_CANONICAL_OPS_ENABLED`、`api-spec §1.5` 中均正常存在且语义一致——它与「15 个 MCP Tool」是不同抽象层的两个概念（12 操作中仅 create/search/delete 三个直接映射为 MCP 工具，其余经 REST 暴露），架构 §7.1a「MCP Bridge 集成（勘误）」段已完整说明二者关系。上轮将其判为「同文档内矛盾」属误诊。

---

## 模块四 · 风险等级汇总

| 编号     | 维度        | 问题                                                              | 风险  |
| :----- | :-------- | :-------------------------------------------------------------- | :-- |
| R18-01 | 1 完整性/一致性 | README 目录边界说明三处口径互斥 + 核心文档数 55 过时                               | 【中】 |
| R18-02 | 1 完整性/一致性 | AP-01~28 编号双重归属；权威定义源在非权威 analysis 目录；命名空间未注册                   | 【中】 |
| R18-03 | 1 完整性/一致性 | claim-matrix AP 追踪节内容空转，声明事实源死链                                 | 【中】 |
| R18-04 | 1 完整性/一致性 | 架构 0.0.41 版本记录行批次号错标 0.0.40                                     | 【低】 |
| R18-05 | 1 完整性/一致性 | 「三链路融合与检索扩展」小节名与四链路内容不符                                         | 【低】 |
| R18-06 | 2 缺失/过时   | OpenAPI 契约结构无效（25 路径参数缺失 / 55 写操作无 body / 88 操作无 security）      | 【中】 |
| R18-07 | 2 缺失/过时   | 契约「Phase 0 补全」承诺无追缴条目、未进 project-plan                           | 【中】 |
| R18-08 | 2 缺失/过时   | .gitignore 未覆盖根目录审计报告                                           | 【低】 |
| R18-09 | 3 结构/组织   | README 未索引 schema-slice.sql / mcp-tools.json；openapi.yaml 无索引条目 | 【中】 |
| R18-10 | 3 结构/组织   | api-contract/ 新目录未登记入 AGENTS.md 目录结构与职责分层                       | 【低】 |
| R18-11 | 3 结构/组织   | 架构 3953 行持续增长，未按 §4.2 执行显式拆分评估                                  | 【低】 |
| R18-12 | 4 格式规范    | §6 零版本标记违规 6 处（5 处为 0.0.43 自引入，含 yaml 注释）                       | 【中】 |
| R18-13 | 4 格式规范    | D-422~425 引入 4 处硬行号；门禁 6.15 正则漏检（doc-audit.py:571）              | 【中】 |
| R18-14 | 4 格式规范    | D-422~425 标题携带批次标记，超出豁免 1 边界                                    | 【低】 |
| R18-15 | 5 核心准确性   | technology-stack §七 语义自指 + 丢失 §7.3.1 权威指针（F2 误诊回归）              | 【中】 |

**合计：0【高】 / 9【中】 / 6【低】 = 15 项。**

---

## 模块五 · 改进建议（具体可落地）

### A. R18-01 + R18-09 —— README 口径与索引一次性收敛（建议最先做）

1. **删除 L21 整段**。它与 L126 职责重复且三项表述全错；保留 L126（表述正确、位置在「分析文档」索引节内更合理）。
2. L126 与 L138 口径已一致，无需改动；仅按 R18-12 去除其批次标记。
3. 在「规格文档」索引表补三行：
   | 路径                                          | 内容                                                       |
   | :------------------------------------------ | :------------------------------------------------------- |
   | `specification/schema-slice.sql`            | **竖切 DDL** — 14 张竖切表可执行建表语句（data-model 指定的 DDL 唯一承载）     |
   | `specification/api-contract/openapi.yaml`   | **REST 契约骨架** — OpenAPI 3.1，81 路径 / 88 操作（骨架，schema 待补全） |
   | `specification/api-contract/mcp-tools.json` | **MCP 工具契约** — 15 工具清单（inputSchema 待补全）                  |
4. 同步 L138 计数式：明确 `.sql` / `.json` / `.html` 三类非 md/yaml 资产的计数政策（建议：列为「附属资产」单列，不并入「核心文档 56 份」，避免每新增一类就要改门禁）。

### B. R18-06 + R18-07 —— 契约产物补正与归属

**最小可用修正**（不需要写完整 schema，即可让文件通过规范校验）：

1. 为 22 条模板路径补 path 级 `parameters` 块（每条一次，覆盖 25 个参数）：
   ```yaml
   parameters:
     - name: id
       in: path
       required: true
       schema: { type: string }
   ```
2. 为 55 个写操作补占位 `requestBody`（`$ref: '#/components/schemas/SkeletonRequest'`，与现有 `SkeletonResponse` 同构）。
3. 补全局 `security: [{ apiKey: [] }]`，管理端点操作级覆盖为 `adminKey`；填充顶层 `tags` 声明。
4. 校验方式：`npx @redocly/cli lint openapi.yaml` 或 `spectral lint`，以「零 error」为交付判据。

**归属修正（不可省略）**：

1. 在 `debt-collection.md` 新增 **D-426 接口契约 schema 补全**（五段式：到期日 = Phase 0 结束；改动量 = 88 操作的 request/response schema + 15 工具 inputSchema；不做的后果 = 契约无法用于 codegen/Mock/CI 校验，Markdown 与实现漂移无检测手段；状态 = 未闭环）。
2. 在 `project-plan.md` Phase 0 W2 交付物追加「接口契约骨架补全（openapi.yaml 通过 lint 零 error + mcp-tools.json inputSchema 补全）」，验证列写「`redocly lint` 零 error」。

### C. R18-02 + R18-03 —— AP 编号体系定调（三选一，建议选方案 1）

- **方案 1（推荐·成本最低）**：**废止 AP- 作为声明编号**，将其降格为「吸收批次的内部提案编号」，仅存在于 analysis 目录。权威文档中删除 AP 编号引用（架构版本记录改为直述机制名 + 章节，claim-matrix 删除 AP 追踪节）。理由：这 22 条机制已在架构正文完整落地并被门禁 6.14 机制名映射覆盖，无需第二套编号；认知层 8 条如需追踪，应并入既有 C- 体系。
- **方案 2**：保留 AP 体系，但必须同时做四件事——① 在 `documentation-governance.md` §5 注册 AP- 命名空间；② 将 AP-01~28 的定义表**迁入权威目录**（建议 `references/` 或 claim-matrix 附录）；③ 消解双重归属（认知声明与架构机制各用独立子段或改用 APC-/APM- 前缀）；④ doc-audit 补 AP 编号连续性与断号检查。
- **方案 3（下策）**：维持现状但在 claim-matrix 明确标注「AP 为过程编号，非声明事实源，不得跨文档引用」。

无论选哪个方案，**claim-matrix 中 8 行同文的占位内容必须替换为实际声明文本或删除**——空转的追踪表比没有表更有害。

### D. R18-12 + R18-14 —— 零版本标记清扫与格式收敛

1. 删除 6 处违规标记的批次部分，保留说明性内容。示例：  
   `> **分析产物目录说明（审计报告 F8，changelog 0.0.43）**：…` → `> **分析产物目录说明**：…`
2. openapi.yaml 头部注释删除 `(changelog 0.0.43, 审计报告 F5)`，保留 `AUTO-GENERATED from docs/specification/api-spec.md` 与 SKELETON 警告。
3. D-422~D-425 标题去掉 `，0.0.43 追缴`（信息已在「历史背景」字段）。
4. 一并评估 `integration-design.md` L93、`engineering-workflow.md` L68；在 §6 明确 `documentation-governance` 执行记录段的豁免地位，避免下轮反复讨论。
5. 清扫后执行：`grep -rn "changelog 0\.0\." docs --include=*.md --include=*.yaml | grep -v changelog.md | grep -v "^.*| 0\.0\."`，结果应仅剩版本记录表行。

### E. R18-13 + R18-15 —— 硬行号与误诊回归修正

1. **R18-13**：D-422~D-425 的「架构 L1496/L1993/L2472/L3388」改为语义引用——分别为 `架构 §3.9 检索深度分级`、`架构 §5.2 升华管道`、`架构 §5.5`、`架构 §9.2`（本轮已逐一反查确认对应关系）。
2. **R18-15**：`technology-stack.md` L95 回退为语义正确的表述，例如：
   > Kairos 对外暴露符合 MCP 规范的 Tool 接口，将 [12 规范操作集](../foundation/architecture-v0.1.0.md) §7.3.1 中的 create/search/delete 直接映射为 MCP Tool，连同检索/维护/治理类 9 个与关系管理 3 个，共暴露 15 个 MCP Tool（构成口径见 [架构 §7.1a](../foundation/architecture-v0.1.0.md)）。
   L114 的 SDK 一致性表述改为 15 个 MCP Tool 是合理的，可保留。
3. 在 changelog 0.0.44 明确登记「F2 判定更正：12 规范操作集与 15 MCP Tool 非矛盾，上轮为误诊」——误诊结论若不更正，会被后续轮次当作既定事实继承。

### F. 低风险项快速处置

- **R18-04**：架构 L3951 版本记录说明文字 `changelog 0.0.40` → `changelog 0.0.41`。
- **R18-05**：架构 L2425 小节名改「链路融合与检索扩展」（去掉数量词，避免下次链路数变化再改名）；L3037 术语消歧段「三链路（语义/共现/kNN）」补因果链路或改为「链路集」表述。
- **R18-08**：`.gitignore` 追加 `/*-audit-report.md` 与 `/*-deep-audit.md`。
- **R18-10**：`AGENTS.md` §2.2 目录树补 `api-contract/`；§4.1 职责分层说明 specification 下契约子目录的定位（机器可读契约，以 api-spec.md 为权威描述）。
- **R18-11**：下批次对架构文档做一次显式拆分评估并把结论（拆或不拆、理由）写入 changelog，履行治理 §4.2 的「变更时评估」义务。

### G. 门禁与流程增强（预防复发，价值高于单点修复）

本轮 15 项问题中，**至少 9 项本可由门禁自动拦截**。建议在 `scripts/doc-audit.py` 补以下子检查：

| 新增检查                  | 拦截目标   | 实现要点                                                                                 |
| :-------------------- | :----- | :----------------------------------------------------------------------------------- |
| 6.20 正文批次标记扫描         | R18-12 | 匹配 `changelog\s*0\.0\.\d+`，白名单：changelog.md 全文、各文档「## 版本记录」表格区间、debt-collection 账目字段 |
| 6.15 扩展               | R18-13 | 现有正则补 `(?:架构\|文档\|见)\s*L\d{2,4}` 分支（doc-audit.py:571）                                |
| 6.21 README 索引完整性     | R18-09 | 遍历核心目录全部资产（md/yaml/json/sql/html），断言文件名在 README.md 中出现                               |
| 6.22 README 计数唯一性     | R18-01 | 断言「核心文档 N 份」「184 md」等口径在 README 内**全文唯一**，多处出现即比对一致                                  |
| 6.23 编号命名空间注册         | R18-02 | 扫描全库 `[A-Z]{1,3}-\d{2,3}` 前缀，未在治理 §5 注册表登记即告警                                        |
| 6.24 契约↔api-spec 端点同步 | 新增漂移面  | openapi.yaml 的 `(method, path)` 集合与 api-spec 端点清单双向比对                                |
| 6.25 未闭环承诺追缴          | R18-07 | 扫描「待补全 / Phase X 补全 / 后续版本」措辞，要求 debt-collection 存在对应条目                              |

**流程层（比门禁更重要）**：

1. **修复批次强制回归自检**——`engineering-workflow.md` §三 已有「批次收尾检查清单」，建议追加两条强制项：① 修复引入的新增文本必须自查 §6 零版本标记与硬行号；② 每项修复完成后 grep 全库同模式，确认无同源残留（本轮 F1 遗漏 data-model L81 即因缺此步）。
2. **误诊防护**——审计报告中判定「A 与 B 矛盾」的条目，修复前须先确认 A、B 是否为同一抽象层的同一对象。建议在清单中增加「已确认同层」勾选项。R18-15 的根因正是跳过了这一确认。
3. **核销机制保持**——本轮的 F1–F9 逐项实测证明该机制有效（发现 2 项误诊、2 项空转、2 项引入新缺陷，均为 changelog 自述所掩盖）。建议固化为每轮标准动作，且**核销结论以实测为准，不采信批次自述**。

---

## 附录 · 本轮已验证的「无问题」基线（供下轮免重复审计）

以下项本轮已实测通过，下轮可直接采信（除非期间发生相关改动）：

- **门禁**：doc-audit.py 18 类 + 14a + 6.8a/6.12a/6.13~6.19 全绿（退出码 0）；deep-audit.py 无阻断项。
- **计数口径**：168 功能（43+125，144 为 Phase 0~2 小计已注明）/ 57 表 / 370 参数（224+146）/ 69 术语 / 38 错误码 / 66 操作 / 12 ADR / 70 组件 / 9 E2E / 88 端点（85 `/v1` + 3 非前缀）/ 184 md（53 核心 + 131 analysis）+ 3 yaml。
- **检索权重**：三信号 0.50/0.35/0.15 与四链路 0.50/0.20/0.10/0.20 分属管线两阶段，架构 §7.3a 有显式管线关系声明与术语消歧，configuration 双套配置项独立承载，project-plan / test-plan / concept-tiers 引用一致。
- **AP 机制章节落点**：架构版本记录声称的 §3.9 / §5.2 / §5.5 / §9.2 归属经逐行反查全部属实。
- **MCP 工具清单**：mcp-tools.json 的 15 个工具名与架构 §7.1a、api-spec §6.8 逐一吻合（门禁 6.12a 亦覆盖）。
- **OpenAPI 路径提取**：81 路径 / 88 操作与 api-spec 端点口径一致（结构问题见 R18-06，但路径本身准确）。
- **元数据**：核心文档 53 份 status 全为 draft；updated 分布 08-07×39 / 08-06×11 / 08-05×3，无倒挂。
- **架构文档编码**：3953 行，UTF-8 无 BOM、行尾 LF、无乱码（终端 grep 截断产生的显示乱码经 Read 复核为假象，不构成问题）。

---

## 附录 · 审计方法与证据等级声明

| 结论类型                              | 取证方式                                       | 证据等级        |
| :-------------------------------- | :----------------------------------------- | :---------- |
| 门禁全绿、deep-audit 无阻断               | 本轮实际执行两个脚本                                 | 全量执行        |
| 文档/md/yaml 计数、AP 编号分布、README 索引覆盖 | Python 脚本遍历文件系统 + 全文比对                     | 全量程序化       |
| OpenAPI 结构缺陷（25/55/88 三组数字）       | PyYAML 解析后逐 operation 校验                   | 全量程序化       |
| §6 违规 6 处、硬行号 4 处                 | 全库 grep + 逐条人工判定豁免边界                       | 全量扫描 + 人工判定 |
| F2 误诊、§7.3.1 指针丢失                 | `git diff` 对比修复前后原文 + 架构 §7.3.1/§7.1a 原文核对 | 直接证据        |
| AP 章节落点准确性                        | awk 反查行号所属章节标题，逐条比对                        | 抽样 6 条，全部命中 |
| 核心计数口径（168/57/370/…）              | 依赖门禁 [6] 与 [6.8a] 自动校验 + 关键项人工抽验           | 门禁全量 + 人工抽样 |
| 核心文档设计事实正确性                       | 人工阅读关键章节 + 跨文档交叉比对                         | 抽样审阅，非逐行全量  |

**声明**：本轮未执行全库逐行阅读（184 份 md 中 131 份为 analysis 分析产物，仅作边界核查未审内容）。「维度 5 通过」的结论基于门禁全量校验 + 关键章节抽样审阅，非逐行全量验证。本报告所有行号、数字均于 2026-08-07 基线 0.0.43 实测取得。**本轮仅做只读分析，未修改任何文档。**

