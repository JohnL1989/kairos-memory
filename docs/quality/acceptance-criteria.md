---
title: Kairos 验收标准
aliases:
  - 验收标准
  - Acceptance Criteria
tags:
  - kairos
  - quality
  - verification
created: 2026-07-20
updated: 2026-08-12
last_reviewed: 2026-08-12
status: design-freeze
---

> **章节导航**——按下表定位所需章节：
>
> | 章节 | 主题 |
> |:----|:----|
> | 〇、竖切验收标准 | v0.1.0-slice 首交付硬性判据 |
> | 一、Code Debt 完成条件 | 代码债务闭环判定 |
> | 一a、记忆质量评估指标 | v1.1+ 设计目标 |
> | 二、v0.1.0 发布检查项 | 发布门禁清单 |
> | 三、时序基准参数 | 基准测试参数登记 |
> | 三a、恢复契约验收 | 恢复契约六属性验收 |
>

---

# Kairos 验收标准

> **定位**：定义各项 v1.1+ 代码债务的可判定完成条件，以及 v0.1.0 发布的硬性检查项（债务完整清单见 [debt-collection.md](../governance/debt-collection.md)）。

---

## 〇、竖切（v0.1.0-slice）验收标准

> 竖切范围见 feature-list「竖切范围」标注与 project-plan「一、竖切范围」。以下为竖切完成的硬性判据——全部满足方可进入定稿评审（触发版本号升级 0.0.1 → 0.1.0）。

| 检查项 | 条件 | 验证方法 |
|:------|:----|:---------|
| 功能闭环 | 写入→路径/语义检索→遗忘→复兴→校准→降级全链路可运行 | E2E-01/02/04/05/06/07/08 通过（E2E-03 升华、E2E-09 S-14 语境自指不在竖切范围；S-14 由单测覆盖） |
| 双副本分离 | 使用权重写回见证锚定被拒绝 | S-14 单测 |
| 遗忘闭环 | 单曲线衰减 + 潜伏势能重估 + 复兴加速可观测 | TC-F01~F03 通过 |
| 身份构造论 | 外部校准可触发 is_identity 置信度变更（升降双向），identity_demotion 审计可追溯；S-10 见证豁免不可绕过 | G-03 v0.1.0 判据；S-10 单测 |
| 事件总线 | 4 类事件发布/订阅/背压/优先级可用，trace_id 可审计 | 事件集成测试 |
| 存储后端 | SQLite 后端全功能可用；StorageBackend 接口可替换（mock PG 后端单测通过） | 单元测试 |
| 性能基准（1 万条 SQLite） | 写 P50≤50ms；路径检索 P50≤20ms；语义检索 P50≤100ms | benchmark-plan 3.1/3.2 |
| 质量门禁 | 单元覆盖率 ≥80%；19 条红线逐条单测；doc-audit.py 全绿；RTM 与 feature-list 竖切标注一致 | pytest + `scripts/doc-audit.py` |
| 定稿评审 | 以上全过 + release-guide 检查清单 + 文档定稿检查 | 评审通过 → 版本号升级（0.0.1 → 0.1.0） |

---

## 一、Code Debt 完成条件

### 债务 D-301 逻辑-因果轴完整落地

**验收条件**（全部满足即视为完成）：
1. 因果链完整性检查器：输入一条记忆 ID，可输出该记忆所属因果链的完整节点列表
2. 执行流断裂检测器：程序性记忆的执行步骤序列出现断点时，标记 `execution_flow_broken`
3. 节律记忆时间索引：周期性事件的后续触发预测与实际情况的偏差 ≤ 20%（对短周期事件如日周期的 20% ≈ 4.8h——此时偏差上限实用意义有限，短周期事件以 ±2h 为补充判断——±2h 判据适用于短周期事件（时间跨度 ≤24h），长周期事件以偏差率判据为准）
4. 时间贴现加权：检索排序中物理时间和逻辑-因果时间的加权公式按配置执行

### 债务 D-302 动态身份增强

**验收条件**：
1. 身份注册表定期（每 N 周期，N=30（可配置））适配性审查执行成功
2. 核心/外围区分生效：外围身份可降级（is_identity→false），核心身份不可降级
3. 身份记忆降级门槛由「宪法修订端口」降至「元认知层+宪法解释层联合审批」
4. 所有身份调整事件写入审计日志

### 债务 D-303 能力转化状态机

**验收条件**：
1. procedural::nascent → consolidated → degraded 状态机可被代码追踪
2. 状态转换触发条件（验证计数 + 冷却周期）按配置执行
3. 状态转换事件写入使用事件总线
4. 长期不用的程序记忆可退回至 `degraded` 状态（需要 WM 检索才能重新执行）

### 债务 D-304 定向遗忘机制（已闭环，见 DC-027）

**验收条件**：
1. 通过 CLI/REST API 执行定向遗忘 → 标记路径不可检索
2. 定向遗忘的数据仍保留在存储层（可恢复）
3. 所有定向遗忘操作写入审计日志（含操作者/时间/原因/HMAC）
4. 通过宪法解释层的定向遗忘审查器授权（非自授权）
5. 定向遗忘不逃逸 S-10（不被遗忘的见证仍豁免）

### 债务 D-305 外部校准源充分性建模

**验收条件**：
1. 校准源可靠性评估函数实现（输入：校准信号时间序列 → 输出：可靠性分数）
2. 充分性判据函数实现（当前覆盖维度 vs 所需覆盖维度的差距量化）
3. 充分性不足时发出主动告警（而非仅被动降级）
4. 单 Provider 降级按加权规则执行

### 债务 D-306 四轴完整度量函数

**验收条件**：
1. 反例覆盖度监测器：跟踪已覆盖推理空间边界 / 总边界
2. 路径禁区标注密度监测器：已标注死胡同路径 / 总探索路径
3. 组合约束网络连通性监测器：约束间可达路径数 / 最大可能数
4. 三项指标可通过 API 查询

---

## 一a、记忆质量评估指标（v1.1+ 设计目标）

> **外部理念吸收注记**：评价记忆系统不能只看召回率，至少需覆盖相关性、**过时调用率**、**任务成功率**与成本四项。Kairos 已有相关性（语义检索判据 §二）与成本（benchmark-plan 性能指标）维度，本节补齐缺口的两项「学习效果」指标。
>
> **定位声明**：以下指标为 v1.1+ 设计目标，v0.1.0 不设硬性阈值（同架构 §10.5 占位声明口径——指标值均为理论推导，代码启动后经基准测试验证后晋升）。数据基础在 v0.1.0 已具备（freshness 状态 + usage_events 表 + 检索日志），指标落地不要求新表。

### 过时调用率（Stale Call Rate）

**定义**：检索结果中被遗忘调度器/freshness.py 标记为 `stale` 或 `expired`（或 fact_freshness 中 `status ∈ {stale, expired}`）的记忆，被选中进入最终输出（WM 激活/上下文注入）的比例。分母为全部被选中进入输出的检索结果数，分子为其中过时者数。

**语义**：回答「记忆系统会不会把已经过时的经验当成有效经验使用」——视频案例：处理 2026 年网站时检索到 2024 年但文字高度相似的流程，比较新的弱相似经验更危险。过时调用率是「检索质量 × 遗忘质量」的联合失效面，单看召回率无法暴露。

**v0.1.0 数据基础**：`memories.status`（active/stale/archived）+ `fact_freshness.status` + `usage_events`（检索-选择-结果三元组）。指标计算为离线聚合（Deep 模式日频），不进入实时检索路径。

**阈值（设计目标）**：过时调用率 > 20% 触发质量告警（warning）；> 40% 触发治理面审查（critical）。具体值经首轮基准验证后校准。

### 任务成功率改善（Task Success Rate Improvement）

**定义**：在集成场景（外部 Agent 如 Hermes 使用 Kairos 记忆完成真实任务）下，启用记忆检索相比不启用记忆检索的任务成功率的净改善幅度。

**语义**：回答「记忆让 Agent 变强了没有」——对应视频第1集「学习 = 能力发生可测量的改善」。改善须经**反事实检验**成立才算数（见 [test-strategy.md](test-strategy.md) §2.7）：拿走历史上下文、仅保留系统声称已学到的内容时，改善仍然存在，而非仅靠上下文续写。

**测量方法**：三态对比（完整上下文 / 仅检索记忆 / 无记忆）的 A/B 测试，用例模式见 [test-strategy.md](test-strategy.md) §2.7。任务成功率由外部 Agent 侧反馈回传（Hermes Memory Provider 集成层）。

**测量任务集（0.0.91 补充）**：可选用 LongMemEval 公开数据集作测量任务集（[benchmark-plan.md](benchmark-plan.md) §3.15，六类问题 × 3 条 = 18 条样本，judge 模型 0/1 判定）——公开固定数据集，验证集与经验来源天然分离（满足 §3.11 红线），与反事实检验三态对比组合使用。

**阈值（设计目标）**：记忆态相对无记忆态的成功率净改善 ≥ 10% 判定「记忆有效」；反事实检验中「仅检索记忆」态相对「无记忆」态无显著改善时，判定为伪持续学习（上下文续写型），触发记忆质量审查。

---

## 二、v0.1.0 发布检查项

### 功能检查

| 检查项 | 条件 | 验证方法 |
|:------|:----|:---------|
| 记忆 CRUD | 写入/读取/更新/删除全链路可运行 | E2E 测试通过 |
| 路径空间 | 路径前缀检索和树状浏览正常 | 集成测试 |
| 语义检索 | 向量搜索返回语义相似的记忆；量化判据：top-5 结果中 ≥3 条与查询语义相关（相似度 ≥0.5 或人工确认）。检索**质量**回归基准见 [benchmark-plan.md](benchmark-plan.md) §3.13（隐式关联召回套件，含间接查询用例）与 §3.14（Task-Aware Precision@K / MRR 评分流程）；[benchmark-plan.md](benchmark-plan.md) §3.2 只覆盖检索**延迟**，不承载召回判据 | 精度验证 |
| 双副本分离 | 使用权重写回见证锚定被拒绝（S-14） | 单元测试 |
| 四类契约 | 每类契约的激活/遗忘行为符合定义 | 单元测试 |
| 遗忘调度 | 定时扫描 + 遗忘决策执行 | 集成测试 |
| 升华管道 | raw→item→strategy→behavior 四阶段可追踪 | E2E 测试 |
| 外部校准 | 校准信号接收 + 见证锚定更新 | E2E 测试 |
| 降级模式 | 三模式触发/切换/退出正确 | E2E 测试 |

### 配置集检查

> 依据架构 [architecture-v0.1.0.md](../foundation/architecture-v0.1.0.md) §0.8「命名配置集与组合约束」——三种命名配置集是唯一受支持的编译形态，未命名组合不进入验收范围。

| 检查项 | 条件 | 验证方法 |
|:------|:----|:---------|
| 三配置集启动校验 | `kairos-minimal` / `kairos-slice` / `kairos-full` 均能通过启动校验并正常启动 | 集成测试（[test-strategy.md](test-strategy.md) §6.1） |
| 未命名组合拒绝 | 人为构造未命名标志组合时拒绝启动并输出 `invalid_flag_composition` | 集成测试 |
| 宪法核不可禁用 | `NARRATIVE_IDENTITY=OFF` 时拒绝启动并输出 `constitutional_core_unavailable` | 集成测试 |
| 证伪测试套件 | 12 个特征标志各具至少 1 条 `[FALSIFICATION]` 用例且通过 | [test-plan.md](test-plan.md) §3.10 + pytest-mark |
| 降级阶梯 | H1/H2 证伪失败时按 `kairos-full` → `kairos-slice` → `kairos-minimal` 整体降级，降级后组合仍属合法配置集 | TC-FAL-001/002 |

> **竖切验收形态**：竖切（v0.1.0-slice）以 `kairos-slice` 为验收形态（判据见 §〇）；`kairos-full` 为 v0.1.0 完整交付形态。

### 安全红线检查

| 检查项 | 目标 |
|:------|:----|
| 安全红线 S-01~S-19 | 19/19 单元测试通过 + S-19 行为层 TC-S19-002~004 通过（三样本/双副本/边界断言，见 test-strategy §2.2a） |
| 审计链 HMAC 完整性 | 连续审计日志的 HMAC 链可验证 |
| API Key 认证 | read/write/admin 三级鉴权正确 |

### 非功能检查

| 检查项 | 目标 | 验证方法 |
|:------|:----|:---------|
| 写入延迟 | P50 ≤ 50ms, P95 ≤ 200ms（NFR 目标值） | benchmark-plan 3.1 写入延迟测试 |
| 检索延迟（路径前缀） | P50 ≤ 20ms, P95 ≤ 80ms | benchmark-plan 3.2 检索延迟测试 |
| 检索延迟（语义） | P50 ≤ 100ms, P95 ≤ 300ms | benchmark-plan 3.2 检索延迟测试 |
| 写入吞吐 | ≥ 100 ops/s | benchmark-plan 3.3 吞吐测试 |
| 检索吞吐 | ≥ 180 ops/s（保留 10% 余量；受单客户端读限流 120/min 约束，100 并发理论峰值 ≈200/s。如需更高吞吐须在基准测试中临时调高限流上限，见 [configuration.md](../ops/configuration.md) §7） | benchmark-plan 3.3 吞吐测试 |
| 容量（标准模式） | ≥ 100 万条（N-05，含已归档） | benchmark-plan 二 极限档 1,000,000 容量边界验证 |
| 容量（轻量模式） | ≥ 10 万条（N-06） | benchmark-plan 3.5 磁盘增长（10 万条 ≤ 2GB / ≤500MB）+ 写入/检索计时基线 |
| 恢复 RTO | 自动恢复 ≤ 30s（N-09） | benchmark-plan 3.6 故障恢复测试（kill -9 崩溃注入 + 恢复计时） |
| 恢复 RPO | ≤ 5 分钟（N-10，数据库组件；升华层等可重建产物 ≤1 天见 reliability §二） | benchmark-plan 3.6 RPO 验证（写入→崩溃→全量+WAL 回放→丢失窗口确认） |
| 系统可用性 | ≥ 99.9%（N-11，适用于有进程级恢复的部署） | 运行期 SLO 统计（`kairos_availability_ratio`，见 observability §1.1；基准测试不统计，以生产运行数据为准） |
| 降级可用性 | ≥ 99%（N-12，外部校准中断期间） | 运行期 SLO 统计（`kairos_availability_ratio`，见 observability §1.1） |
| 安全红线 | S-01~S-19 全部单元测试通过 + S-19 行为层无痕验证（§2.2a） | test-strategy 2.2 + 2.2a |

### 文档检查

| 检查项 | 条件 |
|:------|:----|
| 认知基础 | `status: draft`，版本记录完整 |
| 架构文档 | `status: draft`，版本记录完整 |
| 补充文档 | 功能清单/数据模型/接口规格/测试策略/测试计划/基准计划/安全规格/威胁模型/技术选型/开发规范/项目计划 齐备 |
| README | 所有文档入口注册、路径正确 |

### 记忆系统成熟度五问评估清单（外部理念吸收）

> **外部理念吸收注记**：评估记忆系统不能只看「检索准不准」——还需回答「系统作为记忆体的成熟度」：原始事实是否还在、加工产物能否回溯、读写决策是否显式、审计是否可查、回放是否可复现。以下五问作为 v0.1.0 验收检查项，全部通过视为记忆系统成熟度合格。

| # | 评估问题 | 验收判据 |
|:--|:--|:--|
| ① | **原始事实是否保留？** | 原始轮次/未加工内容在升华、压缩、改写后仍可回溯——`journal_entries` 保留原始内容、`memory_versions` 快照可回滚、升华产物 provenance 链完整（原始事实 → 加工产物可溯源） |
| ② | **视图是否可回溯？** | 派生视图（摘要、embedding、关系边、kNN 近邻）可回溯至生成源记忆——`compression_trail` / `derived_from` 溯源链可查；重建后结果一致（索引为派生视图可重建，见 detailed-design §2 写入管线设计） |
| ③ | **读写决策是否显式？** | 每次写入/修改/遗忘/压缩决策显式可查（谁、何时、为何）——`audit_log` + `memory_states` + `usage_events` 覆盖决策轨迹，无静默写入路径 |
| ④ | **审计是否可查？** | 审计链 HMAC 完整性可验证——安全红线 S-01~S-19 单元测试通过 + 审计链连续性校验通过 |
| ⑤ | **回放是否可复现？** | 同一输入序列的回放结果可复现——恢复契约六属性（见 detailed-design §10.5）验收通过；基准测试可复现（见 [benchmark-plan.md](benchmark-plan.md) §四 统计方法） |

---

## 三、时序基准参数（基准测试验证见架构 [architecture-v0.1.0.md](../foundation/architecture-v0.1.0.md) §10.5 量化指标；参数登记见 [ops/configuration.md](../ops/configuration.md) 附录 A 全库参数总索引）

> 以下参数定义时序基准测试的配置。架构文档 [architecture-v0.1.0.md](../foundation/architecture-v0.1.0.md) §10.5 仅声明「代码启动后首个迭代执行基准测试、验证通过后晋升为已验收指标」，本表为此处参数权威，架构文档中不再保留参数表。

| 参数 | 默认值 | 说明 |
|:-----|:------|:-----|
| `KAIROS_BENCHMARK_TEMPORAL_GRANULARITY` | `day` | observation 窗口粒度（`day`/`week`/`month`） |
| `KAIROS_BENCHMARK_OFF_BY_ONE_SCORE` | 0.80 | off-by-one 容忍得分 |
| `KAIROS_BENCHMARK_OFF_BY_TWO_SCORE` | 0.50 | off-by-two 容忍得分 |
| `KAIROS_BENCHMARK_SAME_CHAIN_SCORE` | 0.30 | 同 observation 链追溯得分 |
| `KAIROS_BENCHMARK_TASK_SCORE_WEIGHTS` | `[0.40, 0.30, 0.20, 0.10]` | 四维度权重向量 |

## 三a、恢复契约验收（自 detailed-design §10.5 承接）

> 持久化/防抖相关设计（断点续训检查点、防抖任务链 `debounced_tasks`、`journal_buffer` 回放）的恢复行为验收判据（机制见 [detailed-design.md](../specification/detailed-design.md) §10.5）——任一属性不满足即视为恢复契约违约：

| 属性 | 验收判据 |
|:--|:--|
| **前缀延续**（Prefix Continuity） | 恢复从检查点进度游标继续，已完成前缀步骤不重放（幂等续训） |
| **效果恰好一次**（Effect Exactly-Once） | 恢复/重放产生的副作用（写入、事件发出、外发动作）恰好一次——不重复、不遗漏 |
| **分叉确定性**（Fork Determinism） | 同一检查点 + 同一输入序列的恢复结果唯一确定，无随机分叉 |
| **检查点有效性**（Checkpoint Validity） | 恢复前校验检查点 checksum（`training_checkpoints.checksum`）——损坏检查点拒绝加载，回退最近有效检查点 |
| **消费一次**（Consume-Once） | 待处理队列/防抖任务链条目恢复后消费一次——不重复执行、不静默丢弃（`debounced_tasks.status` 全程可追溯） |
| **恢复确定性**（Recovery Determinism） | 恢复后的系统状态与未中断运行的等价状态一致（差异可验证，如基准重放比对） |

---
## 版本记录

> 草稿阶段从 0.0.1 起；发生实质性内容变更时按 0.0.2 → 0.0.3 … 递增，并在本表登记变更原因；待定稿后升级版本号。

| 版本 | 日期 | 说明 |
|:----|:----|:-----|
| 0.0.1 | 2026-07-31 | 验收标准：债务完成条件、v0.1.0 发布检查项与时序基准参数。 |
| 0.0.2~0.0.41 | 2026-08-07 | （合并占位：changelog 0.0.2~0.0.41 批次的变更未逐条登记于本文档，见 [changelog.md](../governance/changelog.md) 全景） |
| 0.0.42 | 2026-08-07 | 0.0.42 文档审计修复批次（changelog 0.0.42）：§三 标题悬空引用修正（rl-weight-spec → 架构 §10/configuration §9）；新增 §三a 恢复契约验收（承接 detailed-design §10.5）；§二 文档检查清单补基准计划/威胁模型。 |
| 0.0.55 | 2026-08-08 | round24 全面深度审计修复批次（changelog 0.0.55）：认知基础去版本化 30 处改写；引用错位修正（api-spec §6.5 等）；S-19 行为层验收承载；CLI 追缴对齐；blueprint 无编号承诺追缴 D-433~D-438 补登；摘要表 D-422~D-428 补行。 |
| 0.0.59 | 2026-08-08 | round26 全面深度审计修复批次（changelog 0.0.59，补登）：Q-04 语义检索验收行原指 benchmark-plan §3.2（仅检索延迟，不承载召回），改指 §3.13（隐式关联召回套件）与 §3.14（Task-Aware Precision@K / MRR），并注明「§3.2 只覆盖延迟」。 |
| 0.0.65 | 2026-08-08 | round31 深度审计修复批次（changelog 0.0.65）：§二 非功能检查补容量（N-05 标准 ≥100 万 / N-06 轻量 ≥10 万）、恢复 RTO（N-09）、恢复 RPO（N-10）、系统可用性（N-11）、降级可用性（N-12）验收行——对齐 benchmark-plan 3.5/3.6 与 observability SLO 统计。 |
| 0.0.66 | 2026-08-09 | round32 全面深度审计修复批次（changelog 0.0.66）：版本记录补登批次——0.0.59 行（语义检索验收基准修正）为前序批次实质变更漏登记，本批补登（governance §4「触及即登记」）；frontmatter updated/last_reviewed 同步 2026-08-09。 |
| 0.0.75 | 2026-08-09 | round39 全面深度审计修复批次（changelog 0.0.75）：§三 时序基准参数标题与引言修正——「架构 §10 基准测试配置；参数登记见 configuration §9」→「基准测试验证见架构 §10.5 量化指标；参数登记见 configuration 附录 A」（configuration §9 为 RL 权重优化器参数，`KAIROS_BENCHMARK_*` 实际登记于附录 A 全库参数总索引，引用落点错误）；frontmatter updated/last_reviewed 同步 2026-08-09。 |
| 0.0.80 | 2026-08-09 | round42 全面深度审计修复批次（changelog 0.0.80）：引用/口径收口 + 格式收尾 + 术语登记（glossary 70→76）——详见 changelog 0.0.80 叙述节。 |
| 0.0.87 | 2026-08-10 | round49 全面深度审计修复批次（changelog 0.0.87）：补章节导航表。 |
| 0.0.91 | 2026-08-11 | 外部理念吸收落地批次（changelog 0.0.91）：§一a 任务成功率改善补测量任务集（LongMemEval 公开数据集可作测量任务集，指针 benchmark-plan §3.15）。 |
| 0.0.95 | 2026-08-11 | round55 Obsidian frontmatter 闭合修复批次（changelog 0.0.95）：frontmatter 补立即闭合 `---`（章节导航引用块此前被卷入 YAML 区导致 Obsidian 无效属性）；frontmatter updated/last_reviewed 同步 2026-08-11。 |
| 0.1.0 | 2026-08-12 | 定稿评审通过，版本统一升级（0.0.x → 0.1.0）——首版发布（见 changelog 0.1.0 批次） |


