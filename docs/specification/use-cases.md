---
title: Kairos 使用场景
aliases:
  - 使用场景
  - Use Cases
tags:
  - kairos
  - design
  - requirements
created: 2026-07-20
updated: 2026-08-06
last_reviewed: 2026-08-06
status: draft
---

# Kairos 使用场景

> **定位**：描述 Agent 与 Kairos 交互的典型场景。功能清单说「能做什么」，用例说「在什么情境下按什么顺序做」。
>
> **竖切边界注记**：本文 8 个场景覆盖完整 v0.1.0 能力，部分场景依赖的功能不在竖切首迭代（如场景 5 依赖的 suppressed 定向遗忘——承载功能 M-10 属扩展功能，竖切记忆管理仅 M-01/M-03/M-05；场景 2 的异步巩固管道对应 SYS-01 journal 双重隔离写入，竖切已含）。场景与竖切范围的对应以 [feature-list.md](feature-list.md) 竖切标注为准——场景可先于竖切验证（人工/沙箱），竖切验收以 acceptance-criteria 竖切验收标准为准。

---

## 场景 1：冷启动——首次运行与种子注入

**场景**：用户首次启动 Kairos，系统处于冷启动阶段。

**流程**：
1. 用户通过 CLI 执行 `kairos init`（或首次运行时自动触发）
2. 宪法主权面的外部校准端口读取环境变量中的种子锚点配置
3. 种子锚点注入存储层（标记 `provenance=system_generated`）
4. 元认知层记录种子生命周期初始状态（最小化检查通过）
5. 系统进入冷启动期——探索预算初始分配为最大值

**验证**：写入一条记忆 → 检索成功 → 路径空间可访问

## 场景 2：日常写入与检索

**场景**：Agent 在运行过程中写入新经验并检索已有记忆。

**流程**：
1. Agent 通过 Tool `memories_write` 向路径 `kairos://_session/{id}/` 写入记忆
2. 接入层验证输入（长度、路径格式、API Key）
3. 存储层创建记忆记录 + 提交嵌入计算（异步巩固管道执行，嵌入就绪前检索降级为路径前缀匹配）+ 写入路径索引
4. 使用事件总线发送 `use_event` 事件
5. 后续检索：Agent 调用 `memories_search` 按路径前缀或语义查询
6. 结果经权重调制后返回

**变量**：
- 指定契约类型（permanent/ondemand/environmental/temporary）
- 附带情感 VAD 元数据
- 标注与其他记忆的关系

## 场景 3：升华触发与审批

**场景**：系统空闲期间自动执行升华，将原始经验提纯为行为规则。

**流程**：
1. 调度器检测到系统空闲（无活跃检索/写入持续 N 周期）
2. 从使用负载低的记忆中选取升华候选（`provenance!=exploration`）
3. raw → item：提取核心模式，去语境化
4. item → strategy：归纳为可复用的策略
5. strategy → behavior：生成行为规则，进入 `awaiting_approval` 状态
6. 用户收到审批通知 → 手动确认或拒绝
7. 确认后 behavior 生效，标记 `provenance=internal_inference`
8. 升华日志写入使用事件总线

## 场景 4：外部校准介入

**场景**：用户/外部系统发现记忆的见证锚定不准确，发送校准信号修正。

**流程**：
1. 用户通过 REST API `POST /v1/calibrate` 发送校准信号（memory_id + score + source）
2. 宪法主权面外部校准端口接收并鉴权
3. 校准信号写入见证锚定主副本（`narrative_coherence_score` 更新）
4. 差异检验：使用权重影子副本与更新后的见证锚定比对
5. 若无冲突：校准信号注入完成
6. 若有冲突：使用权重标记 `suspect_flag=true`，转入沙箱验证环
7. 事件总线广播 `calibration_signal` 事件

## 场景 5：遗忘与复兴

**场景**：系统自动遗忘低价值记忆，被遗忘的记忆因再次需要而复兴。

**流程**：
1. 遗忘调度器扫描存储层：按 freshness 曲线（`2^(-days_since_last_access / HALF_LIFE)`）计算每条记忆的新鲜度
2. freshness 低于 stale 阈值的记忆进入遗忘队列（状态 `stale`——降权检索）
3. freshness 低于归档阈值的记忆执行归档（`status=archived`，移入冷存储，数据保留可复兴；临时契约记忆由 forgetAfter 硬删除，见架构 §5.2）
4. 一段时间后：元认知层盲区扫描或前向关联命中该记忆（潜伏势能重估端口 `latent_trigger`），或外部校准信号到达
5. 命中 → 触发复兴加速通道（盲区匹配的记忆影子副本置信度非零初始）
6. 记忆恢复为 `status=active`，激活权重设为初始值

> **复兴触发口径（勘误）**：Archived→Active 的复兴仅由**潜伏势能重估端口（盲区驱动/前向关联扫描）或外部校准信号**触发（架构 §5.2 状态转换表）——显式检索仅更新 `last_access_at`，不直接触发状态复兴（与 feature-list F-03、test-plan TC-F03-001 同步修正）。

## 场景 6：外部校准中断与降级

**场景**：长时间无外部校准信号到达，系统进入自动降级。

**流程**：
1. 元认知层健康计数器监测外部校准端口的信号间隔
2. 中断持续 < N 周期：**保守静默**——使用权重停止积累，仅处理显式检索
3. 中断持续 N-M 周期：**受限交叉验证**——允许高置信度记忆间关联验证
4. 中断持续 > M 周期：**安全休眠**——暂停主动推理，仅维持数据完整性
5. 外部校准信号恢复 → 所有降级期间产生的待审查产物进入差异检验（N、M 为配置参数，详见 [architecture-v0.1.0.md](../foundation/architecture-v0.1.0.md) §10.9 降级状态机与 [ops/configuration.md](../ops/configuration.md) 中 `DEGRADATION_PERIOD_N`/`DEGRADATION_PERIOD_M`）
6. 未通过者回滚 → 通过者正常合并 → 系统退出降级模式

## 场景 7：安全红线触发

**场景**：S-14 触发——使用权重试图反向写回见证锚定。

**流程**：
1. WM 层处理检索结果时，发现某记忆的使用权重陡升
2. 差异检验流程启动，但合并逻辑错误地尝试写回见证锚定
3. 存储层检测到写回操作 → 触发 S-14 安全红线
4. 操作被拒绝，审计日志记录时间戳/操作者/目标记忆/红线编号
5. HMAC 链更新
6. 元认知层偏置监测器收到告警（来源收敛 + 校准衰减检查）
7. 宪法解释层评估是否需要人工介入

## 场景 8：种子退化

**场景**：种子锚点在系统运行时因探索-种子冲突而触发退化。

**流程**：
1. 探索活动产出的记忆与种子锚点的价值判断持续冲突
2. 冲突信号累积 → 达到预设阈值
3. 元认知层种子生命周期追踪触发权重衰减评估请求
4. 宪法解释层裁定：执行提前退化
5. 种子锚点权重从特殊值衰减为普通置信度级别
6. 偏置重置选项提供（清除种子衍生的累积偏置，保留审计基线）
7. 日志写入使用事件总线，标记 `exploration_budget`

---

> **覆盖范围声明**：本文 8 个场景覆盖 v0.1.0 竖切核心路径；技能/Playbook/端云同步/压缩/叙事线/三信号混合检索等 2026-08 新增能力的场景待 v1.1 补充。

---
## 版本记录

> 草稿阶段从 0.0.1 起；发生实质性内容变更时按 0.0.2 → 0.0.3 … 递增，并在本表登记变更原因；待定稿后升级版本号。

| 版本 | 日期 | 说明 |
|:----|:----|:-----|
| 0.0.1 | 2026-07-31 | 使用场景：8 个典型交互场景（冷启动/写入检索/升华/校准/遗忘/降级/安全/种子）。 |
| 0.0.2 | 2026-08-04 | 全库深度审计修复：补覆盖范围声明（8 场景覆盖 v0.1.0 竖切核心路径，新增能力场景待 v1.1）。 |
| 0.0.11 | 2026-08-04 | 开发就绪度修复批次：竖切边界注记。 |
| 0.0.14 | 2026-08-05 | 开发就绪度审计修复批次（changelog 0.0.14）：场景 5 遗忘流程改写为 freshness 口径+复兴触发勘误；场景 6 步骤编号修复（括注移入步骤 5）。 |
| 0.0.37 | 2026-08-06 | round15 深度审计修复批次：版本注记纪律收敛（竖切边界注记/复兴触发口径去版本号前缀，保留信息内容）。 |
| 0.0.38 | 2026-08-06 | round16 全面深度审计修复批次（changelog 0.0.38）：路径空间统一下划线命名。 |
