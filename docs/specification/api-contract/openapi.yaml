# Kairos Memory API — OpenAPI 3.1 SKELETON
# AUTO-GENERATED from docs/specification/api-spec.md（权威描述源）。
# 骨架口径：
#   1. 路径 / 方法 / 权限（x-kairos-required-role）/ 版本边界（x-kairos-delivery）/ 成功响应码
#      均提取自 api-spec.md 的端点声明，不含推测；
#   2. request / response schema 已全量补全（D-428 闭合：88 操作 schema 落地，redocly lint 零 error）；
#   3. 错误响应码 = api-spec 端点「错误」条目声明值 ∪ 方法族基线（读方法 / 写方法 / 删除方法各一套）。
#      每个响应的 description 标注来源——「api-spec 声明」为实证项，「方法族基线」为骨架近似，
#      Phase 0 须逐端点收敛为精确集合；
#   4. 认证为单一 bearerAuth（Authorization: Bearer <key>），read/write/admin 三级由 Key 本身承载，
#      不存在独立的 admin 请求头。未标注 x-kairos-required-role 的操作＝api-spec 尚未声明其权限级别，
#      不代表免鉴权（全局 security 仍生效），Phase 0 须补齐（债务 D-428）。
#   5. 请求/响应级头字段（Idempotency-Key 幂等键、If-Match 乐观锁、X-Trace-Id 追踪）与 GET 端点
#      query 参数（?path / ?q / ?limit / ?offset / ?sort / ?clearance 等）为 api-spec 已声明的接口行为，
#      本骨架 parameters/headers 占位待 Phase 0 逐端点补全（债务 D-428）——补齐前不得据此判定
#      接口无参数（api-spec 为行为权威）。
# 切勿将其当作已完成契约；以 api-spec.md 为权威描述，本文件为其机器可读索引。
openapi: 3.1.0
info:
  title: Kairos Memory API
  version: 0.0.59-skeleton
  description: 文档设计阶段（无运行代码）的机器可读接口契约骨架。路径、方法、权限、版本边界与成功响应码提取自 docs/specification/api-spec.md；request/response 体 schema 已全量补全（D-428 闭合，2026-08-12）。
servers:
- url: http://localhost:8010
  description: local dev（默认端口 8010，见 ops/deployment.md、user/quick-start.md）
tags:
- name: audit
  description: 审计与可追溯性端点（S-16 HMAC 链；无 /v1 前缀）
- name: health
  description: 健康检查与存活探针（无 /v1 前缀）
- name: v1
  description: /v1 业务端点（记忆/检索/校准/治理/系统管理）
paths:
  /audit/compression:
    get:
      operationId: get_audit_compression_1
      summary: 逐记忆压缩审计查询（建议七）
      tags:
      - audit
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompressionAuditResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /audit/compression/summary:
    get:
      operationId: get_audit_compression_summary_2
      summary: 压缩审计全局摘要（建议七）
      tags:
      - audit
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompressionSummaryResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /health:
    get:
      operationId: get_health_3
      summary: 健康检查（A01）
      tags:
      - health
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/admin/compaction/rollback/{snapshot_id}:
    post:
      operationId: post_v1_admin_compaction_rollback_snapshot_id_4
      summary: 展开压缩快照
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompactionRollbackRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompactionRollbackResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
      security:
      - bearerAuth: []
    parameters:
    - name: snapshot_id
      in: path
      required: true
      schema:
        type: string
  /v1/admin/compaction/run:
    post:
      operationId: post_v1_admin_compaction_run_5
      summary: 手动触发压缩
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CompactionRunRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CompactionRunResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
      security:
      - bearerAuth: []
  /v1/admin/consistency-check:
    post:
      operationId: post_v1_admin_consistency_check_6
      summary: 手动触发一致性检查
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConsistencyCheckRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConsistencyCheckResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
      security:
      - bearerAuth: []
  /v1/admin/export:
    post:
      operationId: post_v1_admin_export_7
      summary: 导出 .kairos 备份包
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminExportRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminExportResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
      security:
      - bearerAuth: []
  /v1/admin/import:
    post:
      operationId: post_v1_admin_import_8
      summary: 导入 .kairos 备份包
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminImportRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminImportResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
      security:
      - bearerAuth: []
  /v1/audit-log:
    get:
      operationId: get_v1_audit_log_9
      summary: 审计日志查询（CAL05）。以下参数为查询字符串参数：
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/calibrate:
    post:
      operationId: post_v1_calibrate_10
      summary: 发送外部校准信号（CAL01）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalibrateRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalibrateResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/causal:
    post:
      operationId: post_v1_causal_11
      summary: 手动标注因果链路
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CausalLinkRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CausalLinkResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/config:
    get:
      operationId: get_v1_config_12
      summary: 查看配置（A02）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    patch:
      operationId: patch_v1_config_13
      summary: 修改运行时配置（A02）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigUpdateRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/connectors/register:
    post:
      operationId: post_v1_connectors_register_14
      summary: 注册外部平台 Connector
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConnectorRegisterRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectorRegisterResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/constitution:
    post:
      operationId: post_v1_constitution_15
      summary: 宪法级偏好管理（CAL02，需 admin Key）
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConstitutionRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConstitutionResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/degradation/switch:
    post:
      operationId: post_v1_degradation_switch_16
      summary: 降级模式切换（CAL04，需 admin Key）
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DegradationSwitchRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DegradationSwitchResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/edge-types:
    get:
      operationId: get_v1_edge_types_17
      summary: '[v1.1 预留] 查询已注册边类型签名'
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v1.1-reserved
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EdgeTypeListResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（api-spec 声明）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    post:
      operationId: post_v1_edge_types_18
      summary: '[v1.1 预留] 注册新边类型签名'
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v1.1-reserved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EdgeTypeRegisterRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EdgeTypeRegisterResponse'
        '400':
          description: 参数无效（api-spec 声明）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（api-spec 声明）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（api-spec 声明）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/edge-types/{type}:
    put:
      operationId: put_v1_edge_types_type_19
      summary: '[v1.1 预留] 更新边类型合法组合'
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v1.1-reserved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EdgeTypeUpdateRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EdgeTypeUpdateResponse'
        '400':
          description: 参数无效（api-spec 声明）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（api-spec 声明）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（api-spec 声明）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: type
      in: path
      required: true
      schema:
        type: string
  /v1/entities/extract:
    post:
      operationId: post_v1_entities_extract_20
      summary: 从文本提取实体
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EntityExtractRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EntityExtractResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/evolution/{id}:
    get:
      operationId: get_v1_evolution_id_21
      summary: 查询知识演化链
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EvolutionResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/facts:
    post:
      operationId: post_v1_facts_22
      summary: '[v1.1 预留] 直接写入事实三元组'
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v1.1-reserved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FactWriteRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FactWriteResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/falsification:
    get:
      operationId: get_v1_falsification_23
      summary: 证伪信号查询（CAL06）。以下参数为查询字符串参数：
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FalsificationResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/freeze:
    post:
      operationId: post_v1_freeze_24
      summary: 强制冻结（CAL03，需 admin Key）
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FreezeRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FreezeResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/graph/render:
    get:
      operationId: get_v1_graph_render_25
      summary: '[v1.1 预留] 导出知识图谱可视化'
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v1.1-reserved
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphRenderResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/graph/search:
    post:
      operationId: post_v1_graph_search_26
      summary: 实体图谱多跳查询
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraphSearchRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphSearchResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/halls/demote:
    post:
      operationId: post_v1_halls_demote_27
      summary: 将记忆从验证区退回加工区
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HallDemoteRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HallDemoteResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/halls/promote:
    post:
      operationId: post_v1_halls_promote_28
      summary: 将记忆从加工区推进到验证区或正式库
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HallPromoteRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HallPromoteResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/halls/{hall}:
    get:
      operationId: get_v1_halls_hall_29
      summary: 查询指定区域内的记忆列表
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HallListResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: hall
      in: path
      required: true
      schema:
        type: string
  /v1/health/calibration:
    get:
      operationId: get_v1_health_calibration_30
      summary: 校准状态报告（建议一）
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthCalibrationResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/health/detail:
    get:
      operationId: get_v1_health_detail_31
      summary: 聚合健康报告
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthDetailResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/health/memory-pressure:
    get:
      operationId: get_v1_health_memory_pressure_32
      summary: 记忆压力状态（建议四）
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryPressureResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/maintenance/run:
    post:
      operationId: post_v1_maintenance_run_33
      summary: 手动触发后台维护（Light/Deep）
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaintenanceRunRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceRunResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/maintenance/status:
    get:
      operationId: get_v1_maintenance_status_34
      summary: 查看维护引擎运行状态
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaintenanceStatusResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/memories:
    get:
      operationId: get_v1_memories_35
      summary: 记忆检索
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    post:
      operationId: post_v1_memories_36
      summary: 记忆写入
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryWriteRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryCreated'
        '400':
          description: 参数无效（api-spec 声明）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（api-spec 声明）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（api-spec 声明）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（api-spec 声明）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（api-spec 声明）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（api-spec 声明）
  /v1/memories/batch:
    post:
      operationId: post_v1_memories_batch_37
      summary: 批量导入（W03）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchWriteRequest'
      responses:
        '207':
          description: 部分成功（Multi-Status，逐条返回结果） — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BatchWriteResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/memories/heat-top:
    get:
      operationId: get_v1_memories_heat_top_38
      summary: 热度最高记忆（heat_score 降序）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HeatTopResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/memories/merge:
    post:
      operationId: post_v1_memories_merge_39
      summary: 语义合并（保留见证锚定，受 S14 约束）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryMergeRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryMergeResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/memories/search:
    post:
      operationId: post_v1_memories_search_40
      summary: 三信号混合检索（语义 + BM25 + 实体加成）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HybridSearchRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/memories/stats:
    get:
      operationId: get_v1_memories_stats_41
      summary: 记忆库报告（总量/按类型/按状态/增长率）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryStatsResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/memories/{id}:
    get:
      operationId: get_v1_memories_id_42
      summary: 记忆检索
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryDetail'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（api-spec 声明）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（api-spec 声明）
        '404':
          description: 资源不存在（ERR-DB-004）（api-spec 声明）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    patch:
      operationId: patch_v1_memories_id_43
      summary: 记忆更新
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryUpdateRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryDetail'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    delete:
      operationId: delete_v1_memories_id_44
      summary: 清除记忆
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryDeleteResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（api-spec 声明）
        '404':
          description: 资源不存在（ERR-DB-004）（api-spec 声明）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（api-spec 声明）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/archive:
    post:
      operationId: post_v1_memories_id_archive_45
      summary: 归档记忆（竖切功能 M05）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArchiveRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryArchiveResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（api-spec 声明）
        '404':
          description: 资源不存在（ERR-DB-004）（api-spec 声明）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/expire:
    post:
      operationId: post_v1_memories_id_expire_46
      summary: 标记过期（设 TTL，到期自动归档）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryExpireRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryExpireResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（api-spec 声明）
        '404':
          description: 资源不存在（ERR-DB-004）（api-spec 声明）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/export:
    get:
      operationId: get_v1_memories_id_export_47
      summary: 导出记忆（脱敏）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryExportResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/feedback:
    post:
      operationId: post_v1_memories_id_feedback_48
      summary: 可信度反馈
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryFeedbackRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryFeedbackResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/lock:
    post:
      operationId: post_v1_memories_id_lock_49
      summary: 锁定保护（禁止修改/删除）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryLockRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryLockResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（api-spec 声明）
        '404':
          description: 资源不存在（ERR-DB-004）（api-spec 声明）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/restore:
    post:
      operationId: post_v1_memories_id_restore_50
      summary: 归档恢复（竖切功能 M05 配套）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RestoreRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryRestoreResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（api-spec 声明）
        '404':
          description: 资源不存在（ERR-DB-004）（api-spec 声明）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（api-spec 声明）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/rollback:
    post:
      operationId: post_v1_memories_id_rollback_51
      summary: 回滚到指定版本
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryRollbackRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryRollbackResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/suppress:
    post:
      operationId: post_v1_memories_id_suppress_52
      summary: 定向遗忘（抑制检索，保留数据）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemorySuppressRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemorySuppressResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/memories/{id}/versions:
    get:
      operationId: get_v1_memories_id_versions_53
      summary: 获取记忆版本历史
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryVersionsResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/narrative/threads:
    post:
      operationId: post_v1_narrative_threads_54
      summary: 创建叙事线（v0.1.0 子集）
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NarrativeThreadCreateRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NarrativeThreadCreateResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/narrative/threads/{id}/complete:
    post:
      operationId: post_v1_narrative_threads_id_complete_55
      summary: 标记叙事线完结
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NarrativeThreadCompleteRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NarrativeThreadCompleteResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/narrative/threads/{id}/members:
    post:
      operationId: post_v1_narrative_threads_id_members_56
      summary: 添加记忆至叙事线（v0.1.0 子集）
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NarrativeThreadMembersRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NarrativeThreadMembersResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/narrative/threads/{id}/memories:
    get:
      operationId: get_v1_narrative_threads_id_memories_57
      summary: 按叙事线检索记忆（v0.1.0 子集）
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NarrativeThreadMemoriesResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/narrative/threads/{id}/summarize:
    post:
      operationId: post_v1_narrative_threads_id_summarize_58
      summary: 生成叙事线摘要
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NarrativeThreadSummarizeRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NarrativeThreadSummarizeResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/path:
    get:
      operationId: get_v1_path_59
      summary: 路径下记忆列表
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PathListResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/path/rebuild-index:
    post:
      operationId: post_v1_path_rebuild_index_60
      summary: 路径索引重建（A06）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PathRebuildRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PathRebuildResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/path/suppress:
    post:
      operationId: post_v1_path_suppress_61
      summary: 路径级检索抑制（S16/S17）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PathSuppressRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PathSuppressResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/path/tree:
    get:
      operationId: get_v1_path_tree_62
      summary: 路径空间树状浏览
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PathTreeResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/playbooks:
    post:
      operationId: post_v1_playbooks_63
      summary: 创建 Playbook candidate
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaybookCreateRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaybookCreateResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/playbooks/search:
    get:
      operationId: get_v1_playbooks_search_64
      summary: 搜索 Playbook
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaybookSearchResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/playbooks/{id}/feedback:
    post:
      operationId: post_v1_playbooks_id_feedback_65
      summary: 记录 Playbook 使用反馈
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaybookFeedbackRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaybookFeedbackResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/proactive/topics:
    get:
      operationId: get_v1_proactive_topics_66
      summary: 查询待处理主动话题（A17）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProactiveTopicsResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/profile/schema:
    put:
      operationId: put_v1_profile_schema_67
      summary: 注册/更新自定义 Profile Schema
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProfileSchemaRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProfileSchemaResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/reflect:
    post:
      operationId: post_v1_reflect_68
      summary: 对现有记忆执行按需深度分析
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReflectRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReflectResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/scheduler/status:
    get:
      operationId: get_v1_scheduler_status_69
      summary: 调度器状态查询（A03）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchedulerStatusResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/search/explain:
    get:
      operationId: get_v1_search_explain_70
      summary: 检索附带 recall funnel trace
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchExplainResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/seeds:
    get:
      operationId: get_v1_seeds_71
      summary: 种子状态查看（A05）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SeedsListResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    post:
      operationId: post_v1_seeds_72
      summary: 种子锚点管理（需 admin Key）
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SeedCreateRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SeedResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sessions:
    get:
      operationId: get_v1_sessions_73
      summary: 列出最近会话
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionListResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sessions/{id}/messages:
    get:
      operationId: get_v1_sessions_id_messages_74
      summary: 读取会话消息（支持游标分页）
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionMessagesResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    post:
      operationId: post_v1_sessions_id_messages_75
      summary: 批量写入会话消息（Hermes on_session_end 调用）
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionMessageWriteRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SessionMessageWriteResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/skills:
    get:
      operationId: get_v1_skills_76
      summary: '[v1.1 预留] 搜索/列出技能'
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v1.1-reserved
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkillListResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（api-spec 声明）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/skills/{id}/reactivate:
    post:
      operationId: post_v1_skills_id_reactivate_77
      summary: '[v1.1 预留] 重新激活已归档技能'
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v1.1-reserved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SkillReactivateRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkillReactivateResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/skills/{id}/supersede:
    post:
      operationId: post_v1_skills_id_supersede_78
      summary: '[v1.1 预留] 标记技能被替代'
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v1.1-reserved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SkillSupersedeRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkillSupersedeResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
    parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  /v1/sublimation/process:
    post:
      operationId: post_v1_sublimation_process_79
      summary: 处理蒸馏结果（两阶段 API 第二阶段）
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SublimationProcessRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SublimationProcessResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sublimation/prompt:
    post:
      operationId: post_v1_sublimation_prompt_80
      summary: 构建蒸馏提示词（两阶段 API 第一阶段）
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SublimationPromptRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SublimationPromptResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sublimation/status:
    get:
      operationId: get_v1_sublimation_status_81
      summary: 升华进度查询（SF04）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SublimationStatusResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sublimation/trigger:
    post:
      operationId: post_v1_sublimation_trigger_82
      summary: 手动触发升华（SF02）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SublimationTriggerRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SublimationTriggerResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sync/export:
    post:
      operationId: post_v1_sync_export_83
      summary: 导出完整数据快照（.kairos 格式）
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncExportRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncExportResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sync/import:
    post:
      operationId: post_v1_sync_import_84
      summary: 导入数据快照
      tags:
      - v1
      x-kairos-required-role: admin
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncImportRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncImportResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sync/pull:
    post:
      operationId: post_v1_sync_pull_85
      summary: 拉取服务端增量变更
      tags:
      - v1
      x-kairos-required-role: read
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncPullRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncPullResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/sync/push:
    post:
      operationId: post_v1_sync_push_86
      summary: 推送本地增量修改至服务端
      tags:
      - v1
      x-kairos-required-role: write
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncPushRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncPushResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/unfreeze:
    post:
      operationId: post_v1_unfreeze_87
      summary: 解冻
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnfreezeRequest'
      responses:
        '200':
          description: 成功 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnfreezeResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/webhooks:
    post:
      operationId: post_v1_webhooks_88
      summary: '[v1.1 预留] 注册 Webhook 事件订阅'
      tags:
      - v1
      x-kairos-delivery: v1.1-reserved
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WebhookRegisterRequest'
      responses:
        '201':
          description: 已创建 — SKELETON：response schema 待 Phase 0 补全
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookRegisterResponse'
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）（方法族基线，Phase 0 逐端点收敛）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001、ERR-CTR-003）（方法族基线，Phase 0 逐端点收敛）
        '404':
          description: 资源不存在（ERR-DB-004）（方法族基线，Phase 0 逐端点收敛）
        '409':
          description: 状态冲突（ERR-DB-005 / ERR-CNF-001 / ERR-CTR-004）（方法族基线，Phase 0 逐端点收敛）
        '413':
          description: 内容超长（ERR-INPUT-001，64 KB 上限）（方法族基线，Phase 0 逐端点收敛）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）（方法族基线，Phase 0 逐端点收敛）
        '429':
          description: 限流或预算耗尽（ERR-RATE-*）（方法族基线，Phase 0 逐端点收敛）
  /v1/memories/{id}/traces:
    get:
      operationId: get_v1_memories_id_traces_89
      summary: 记忆生命周期轨迹（竖切交付，MCP kairos_get_memory_traces）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      - name: limit
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: 成功 — memory_states 状态机轨迹列表（api-spec §1.4）
        '401':
          description: 认证失败（ERR-AUTH-001/002）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001）
        '404':
          description: 资源不存在（ERR-DB-004）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）
  /v1/relations:
    post:
      operationId: post_v1_relations_90
      summary: 创建关系边（竖切交付，MCP kairos_link）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - from_uri
              - uris
              - reason
              properties:
                from_uri:
                  type: string
                uris:
                  type: array
                  items:
                    type: string
                relation_type:
                  type: string
                  default: reference
                reason:
                  type: string
                strength:
                  type: number
                  default: 1.0
                confidence:
                  type: number
      responses:
        '200':
          description: 成功 — 关系边创建结果（api-spec §1.9）
        '400':
          description: 参数无效（方法族基线，Phase 0 逐端点收敛）
        '401':
          description: 认证失败（ERR-AUTH-001/002）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001）
        '404':
          description: 资源不存在（ERR-DB-004）
        '422':
          description: 语义校验失败（ERR-INPUT-003/004）
  /v1/relations/{memory_id}:
    get:
      operationId: get_v1_relations_memory_id_91
      summary: 查询关系边（竖切交付，MCP kairos_relations）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      parameters:
      - name: memory_id
        in: path
        required: true
        schema:
          type: string
      - name: direction
        in: query
        schema:
          type: string
          enum:
          - inbound
          - outbound
          - both
          default: both
      - name: relation_type
        in: query
        schema:
          type: string
          default: '*'
      - name: limit
        in: query
        schema:
          type: integer
          default: 50
          maximum: 200
      responses:
        '200':
          description: 成功 — 关系边列表（api-spec §1.9）
        '401':
          description: 认证失败（ERR-AUTH-001/002）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001）
        '404':
          description: 资源不存在（ERR-DB-004）
  /v1/relations/{source_id}/{target_id}:
    delete:
      operationId: delete_v1_relations_source_id_target_id_92
      summary: 移除关系边（竖切交付，MCP kairos_unlink）
      tags:
      - v1
      x-kairos-delivery: v0.1.0
      parameters:
      - name: source_id
        in: path
        required: true
        schema:
          type: string
      - name: target_id
        in: path
        required: true
        schema:
          type: string
      - name: relation_type
        in: query
        schema:
          type: string
          default: '*'
      responses:
        '200':
          description: 成功 — 软删除结果（api-spec §1.9）
        '401':
          description: 认证失败（ERR-AUTH-001/002）
        '403':
          description: 权限不足或红线拒绝（ERR-AUTH-003/004、ERR-SEC-001）
        '404':
          description: 资源不存在（ERR-DB-004）
components:
  schemas:
    # =========================================================================
    # 竖切 21 端点 schema（D-428 补全；来源：api-spec.md §1，字段语义以 data-model 为准）
    # =========================================================================
    # --- 记忆写入（POST /v1/memories） ---
    MemoryWriteRequest:
      type: object
      description: 记忆写入请求（api-spec §1.1；S-15 provenance 必填，缺失返回 422）
      required: [path, content, provenance]
      properties:
        path:
          type: string
          description: kairos:// 路径（前缀索引与域路由，架构 §5.2）
        content:
          type: string
          maxLength: 65536
          description: 记忆内容（64KB 硬上限，KAIROS_INPUT_LIMIT_CONTENT_BYTES）
        contract:
          type: string
          enum: [permanent, ondemand, environmental, temporary, intention]
          default: ondemand
          description: 契约五值（架构 §5.2；intention 仅系统内部路径使用）
        memory_types:
          type: array
          items:
            type: string
            enum: [episodic, semantic, procedural]
          description: 记忆类型列表（多重记忆认知模型）
        provenance:
          type: string
          enum: [external_calibration, internal_inference, user_input, system_generated, exploration]
          description: 来源标识（S-15 必填）
        vad:
          type: object
          description: 情感坐标（VAD 三维，各维 [-1,1]）
          properties:
            v: { type: number, format: float, minimum: -1, maximum: 1 }
            a: { type: number, format: float, minimum: -1, maximum: 1 }
            d: { type: number, format: float, minimum: -1, maximum: 1 }
        relations:
          type: array
          description: 关系列表（可选；关系索引表非竖切范围，v0.1.0 忽略）
          items:
            type: object
            properties:
              target_id: { type: string, format: uuid }
              relation_type: { type: string }
              strength: { type: number, minimum: 0, maximum: 1 }
        encoding_context:
          type: object
          description: 编码上下文（时空/任务目标/情感状态，架构 §5.2）
          additionalProperties: true
        occurred_at:
          type: string
          format: date-time
          description: 事件时间（双时态，可空——无法判定时不填）
    MemoryCreated:
      type: object
      description: 记忆写入响应（201；含 version 供 PATCH 乐观锁 If-Match）
      required: [id, path, version]
      properties:
        id: { type: string, format: uuid }
        path: { type: string }
        version: { type: integer, minimum: 1 }
    # --- 批量写入（POST /v1/memories/batch，W-03） ---
    BatchWriteRequest:
      type: object
      description: 批量导入请求（最大 100 条；幂等键语义与单条一致）
      required: [items]
      properties:
        items:
          type: array
          maxItems: 100
          items: { $ref: '#/components/schemas/MemoryWriteRequest' }
        on_conflict:
          type: string
          enum: [skip, overwrite]
          default: skip
          description: 冲突条目逐条跳过/覆盖（207 逐条返回，无 fail 档）
    BatchWriteResponse:
      type: object
      description: 批量导入响应（207 Multi-Status）
      required: [success_count, failed_count, results]
      properties:
        success_count: { type: integer }
        failed_count: { type: integer }
        results:
          type: array
          items:
            type: object
            properties:
              index: { type: integer }
              status: { type: string, enum: [created, skipped, overwritten, error] }
              id: { type: string, format: uuid }
              code: { type: string }
              message: { type: string }
    # --- 记忆对象（GET/PATCH /v1/memories/{id}） ---
    MemoryDetail:
      type: object
      description: 记忆对象（读取/更新响应；字段以 data-model memories 表为权威）
      required: [id, path, version, content, contract, memory_types, provenance, status, created_at]
      properties:
        id: { type: string, format: uuid }
        path: { type: string }
        version: { type: integer, minimum: 1 }
        content: { type: string }
        content_summary: { type: string }
        contract:
          type: string
          enum: [permanent, ondemand, environmental, temporary, intention]
        memory_types:
          type: array
          items: { type: string, enum: [episodic, semantic, procedural] }
        provenance:
          type: string
          enum: [external_calibration, internal_inference, user_input, system_generated, exploration]
        status:
          type: string
          enum: [active, stale, archived, suppressed, superseded]
        is_identity: { type: boolean }
        identity_confidence: { type: number, minimum: 0, maximum: 1 }
        is_sensitive: { type: boolean }
        vad:
          type: object
          properties:
            v: { type: number, minimum: -1, maximum: 1 }
            a: { type: number, minimum: -1, maximum: 1 }
            d: { type: number, minimum: -1, maximum: 1 }
        heat_score: { type: number, minimum: 0, maximum: 1 }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        last_access_at: { type: [string, "null"], format: date-time }
        occurred_at: { type: [string, "null"], format: date-time }
    MemoryUpdateRequest:
      type: object
      description: 记忆更新请求（PATCH；乐观锁强制——须携带 If-Match 请求头（当前版本号），冲突 409 ERR-DB-005）
      properties:
        content: { type: string, maxLength: 65536 }
        content_summary: { type: string }
        vad:
          type: object
          properties:
            v: { type: number, minimum: -1, maximum: 1 }
            a: { type: number, minimum: -1, maximum: 1 }
            d: { type: number, minimum: -1, maximum: 1 }
        memory_types:
          type: array
          items: { type: string, enum: [episodic, semantic, procedural] }
        occurred_at: { type: [string, "null"], format: date-time }
    # --- 归档/恢复（M-05） ---
    ArchiveRequest:
      type: object
      description: 归档原因（可选，写入审计日志）
      properties:
        reason: { type: string }
    RestoreRequest:
      type: object
      description: 恢复原因（可选，写入审计日志；恢复须经潜伏势能重估端口匹配验证）
      properties:
        reason: { type: string }
    # --- 检索（POST /v1/memories/search / GET /v1/memories?q=） ---
    HybridSearchRequest:
      type: object
      description: 三信号混合检索请求（架构 §7.3a：语义 0.50 + BM25 0.35 + 实体 0.15）
      required: [query]
      properties:
        query:
          type: string
          maxLength: 500
          description: 查询内容（KAIROS_INPUT_LIMIT_QUERY_CHARS）
        mode:
          type: string
          enum: [hybrid, semantic, bm25]
          default: hybrid
        weights:
          type: object
          description: 三信号权重（候选域内融合；默认 α_s=0.50/α_b=0.35/α_e=0.15，权重和恒为 1）
          properties:
            semantic: { type: number, minimum: 0, maximum: 1 }
            bm25: { type: number, minimum: 0, maximum: 1 }
            entity: { type: number, minimum: 0, maximum: 1 }
        limit: { type: integer, minimum: 1, maximum: 100, default: 10 }
        filters:
          type: object
          description: 过滤条件（硬过滤边界：路径前缀 + 契约）
          properties:
            contract:
              type: string
              enum: [permanent, ondemand, environmental, temporary, intention]
            path_prefix: { type: string }
    SearchResultItem:
      type: object
      properties:
        id: { type: string, format: uuid }
        path: { type: string }
        content: { type: string }
        contract:
          type: string
          enum: [permanent, ondemand, environmental, temporary, intention]
        score: { type: number, minimum: 0, maximum: 1 }
        created_at: { type: string, format: date-time }
        explanation:
          type: object
          description: 各维度贡献说明（POST /v1/memories/search 返回）
          properties:
            semantic: { type: number }
            bm25: { type: number }
            entity: { type: number }
    SearchResponse:
      type: object
      description: 检索响应（含可选校准状态与 nudge 元数据，api-spec §1.2）
      required: [data, total]
      properties:
        data:
          type: array
          items: { $ref: '#/components/schemas/SearchResultItem' }
        total: { type: integer }
        path: { type: string }
        meta:
          type: object
          description: 校准状态元数据（外部校准静默 >7 天时出现）
          properties:
            calibration_status:
              type: object
              properties:
                level: { type: string, enum: [healthy, degraded, virtual, dormant] }
                last_external_calibration: { type: [string, "null"], format: date-time }
                days_since_last_calibration: { type: integer }
                active_mode: { type: string }
                virtual_calibration_confidence_ceiling: { type: number }
        nudge:
          type: object
          description: 退化态主动校准提示（subtle=7-14 天 / noticeable=14-30 天 / prominent=>30 天）
          properties:
            active: { type: boolean }
            message: { type: string }
            level: { type: string, enum: [subtle, noticeable, prominent] }
            dismissible: { type: boolean }
    # --- 路径操作（GET /v1/path、GET /v1/path/tree） ---
    PathListResponse:
      type: object
      description: 路径下记忆列表（api-spec §1.6）
      required: [data, total]
      properties:
        data:
          type: array
          items: { $ref: '#/components/schemas/MemoryDetail' }
        total: { type: integer }
        path: { type: string }
    PathTreeNode:
      type: object
      properties:
        path: { type: string }
        memory_count: { type: integer }
        children:
          type: array
          items: { $ref: '#/components/schemas/PathTreeNode' }
    PathTreeResponse:
      type: object
      description: 路径空间树状浏览（api-spec §1.6）
      required: [tree, truncated]
      properties:
        tree:
          type: array
          items: { $ref: '#/components/schemas/PathTreeNode' }
        truncated: { type: boolean }
    # --- 校准与治理（POST /v1/calibrate、freeze、unfreeze、degradation/switch） ---
    CalibrateRequest:
      type: object
      description: 外部校准信号（CAL-01；S-11 宪法修订唯一入口）
      required: [memory_id, narrative_coherence_score, source]
      properties:
        memory_id: { type: string, format: uuid }
        narrative_coherence_score: { type: number, minimum: 0, maximum: 1 }
        source: { type: string }
    CalibrateResponse:
      type: object
      required: [status, memory_id]
      properties:
        status: { type: string, enum: [accepted] }
        memory_id: { type: string, format: uuid }
        previous_score: { type: [number, "null"] }
        new_score: { type: number }
    FreezeRequest:
      type: object
      description: 强制冻结（CAL-03，admin Key；到期自动解冻）
      required: [duration_seconds]
      properties:
        duration_seconds: { type: integer, minimum: 1 }
        scope:
          type: string
          enum: [all]
          default: all
    DegradationSwitchRequest:
      type: object
      description: 降级模式切换（CAL-04，admin Key；三模式状态机见架构 §10.9）
      required: [mode]
      properties:
        mode:
          type: string
          enum: [conservative_silent, limited_cross_validation, safe_hibernation]
    # --- 审计日志（GET /v1/audit-log，CAL-05） ---
    AuditLogEntry:
      type: object
      description: 审计记录（HMAC 链条目，S-16；字段以 data-model audit_log 为权威）
      properties:
        id: { type: integer }
        timestamp: { type: string, format: date-time }
        operator: { type: string }
        action: { type: string }
        target_type: { type: [string, "null"] }
        target_id: { type: [string, "null"] }
        content_hash: { type: [string, "null"] }
        previous_hash: { type: [string, "null"] }
        hmac: { type: string }
        details: { type: [object, "null"] }
        redline_id: { type: [string, "null"] }
    AuditLogResponse:
      type: object
      required: [logs, chain_valid, total]
      properties:
        logs:
          type: array
          items: { $ref: '#/components/schemas/AuditLogEntry' }
        chain_valid: { type: boolean, description: HMAC 链完整性校验结果 }
        total: { type: integer }
    # --- 系统管理（GET /health、GET/PATCH /v1/config、GET/POST /v1/seeds） ---
    HealthResponse:
      type: object
      description: 健康检查（A-01，GET /health 无 /v1 前缀）
      required: [status, components]
      properties:
        status: { type: string, enum: [ok, degraded, down] }
        components:
          type: object
          properties:
            api: { type: string }
            db: { type: string }
            scheduler: { type: string }
            embedding: { type: string }
            sublimation: { type: string }
        uptime_seconds: { type: integer }
    ConfigEntryItem:
      type: object
      properties:
        key: { type: string }
        value: { type: string }
        scope: { type: string, enum: [static, dynamic, override] }
        updated_at: { type: string, format: date-time }
    ConfigResponse:
      type: object
      required: [config]
      properties:
        config:
          type: array
          items: { $ref: '#/components/schemas/ConfigEntryItem' }
    ConfigUpdateRequest:
      type: object
      description: 运行时配置修改（A-02；仅接受竖切参数族内参数，越界 400）
      required: [updates]
      properties:
        updates:
          type: array
          items:
            type: object
            required: [key, value]
            properties:
              key: { type: string, pattern: '^KAIROS_[A-Z][A-Z0-9_]*$' }
              value: { type: string }
    SeedCreateRequest:
      type: object
      description: 种子锚点创建（admin Key；种子生命周期约束见架构 §1.2）
      required: [seed_type, path, initial_confidence, current_confidence]
      properties:
        seed_type: { type: string, enum: [config, identity, calibration] }
        path:
          type: string
          description: 种子路径（kairos://_system/seeds/ 前缀）
        content: { type: object, additionalProperties: true }
        initial_confidence: { type: number, minimum: 0, maximum: 1 }
        current_confidence: { type: number, minimum: 0, maximum: 1 }
    SeedItem:
      type: object
      properties:
        path: { type: string }
        seed_type: { type: string, enum: [config, identity, calibration] }
        status: { type: string, enum: [active, degrading, retired] }
        degradation_level: { type: number, minimum: 0, maximum: 1 }
        initial_confidence: { type: number }
        current_confidence: { type: number }
        review_count: { type: integer }
    SeedResponse:
      type: object
      required: [seed]
      properties:
        seed: { $ref: '#/components/schemas/SeedItem' }
    SeedsListResponse:
      type: object
      required: [seeds]
      properties:
        seeds:
          type: array
          items: { $ref: '#/components/schemas/SeedItem' }
    # =========================================================================
    # 非竖切端点占位（全量 88 操作补全随 Phase 0 收尾，D-428）
    # =========================================================================
    MemoryLeveledRead:
      type: object
      description: api-spec 端点 /v1/memories/{id} 请求/响应 schema（D-428 收尾补全）
      properties:
        id: { type: string }
        level: { type: string, enum: ["summary", "overview", "full"] }
        content: { type: string }
        path: { type: string }
        session:
          type: object
          properties:
            id: { type: string }
            label: { type: string }
        metadata:
          type: object
          properties:
            created_at: { type: string }
            contract: { type: string }
            version: { type: integer }
      required: [id, level]
    SublimationPromptRequest:
      type: object
      description: api-spec 端点 /v1/sublimation/prompt 请求/响应 schema（D-428 收尾补全）
      properties:
        session_id: { type: string }
        records: { type: array, items: {"type": "object", "properties": {"role": {"type": "string"}, "content": {"type": "string"}}} }
      required: [session_id, records]
    SublimationPromptResponse:
      type: object
      description: api-spec 端点 /v1/sublimation/prompt 请求/响应 schema（D-428 收尾补全）
      properties:
        prompt: { type: string }
        record_ids: { type: array, items: {"type": "string"} }
      required: [prompt, record_ids]
    SublimationProcessRequest:
      type: object
      description: api-spec 端点 /v1/sublimation/process 请求/响应 schema（D-428 收尾补全）
      properties:
        raw_response: { type: string }
        record_ids: { type: array, items: {"type": "string"} }
        session_id: { type: string }
        target_level: { type: string, enum: ["L0", "L1", "L2", "L3", "L4"] }
      required: [raw_response, record_ids, session_id, target_level]
    SublimationProcessResponse:
      type: object
      description: api-spec 端点 /v1/sublimation/process 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["accepted", "completed", "failed"] }
        output: { type: object }
      required: [status]
    SublimationStatusResponse:
      type: object
      description: api-spec 端点 /v1/sublimation/status 请求/响应 schema（D-428 收尾补全）
      properties:
        queue: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "stage": {"type": "string"}, "status": {"type": "string"}}} }
      required: [queue]
    SublimationTriggerRequest:
      type: object
      description: api-spec 端点 /v1/sublimation/trigger 请求/响应 schema（D-428 收尾补全）
      properties:
        path: { type: string }
        target_stage: { type: string, enum: ["strategy", "behavior"] }
      required: [path]
    SublimationTriggerResponse:
      type: object
      description: api-spec 端点 /v1/sublimation/trigger 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["queued", "running"] }
        job_id: { type: string }
      required: [status]
    SessionListResponse:
      type: object
      description: api-spec 端点 /v1/sessions 请求/响应 schema（D-428 收尾补全）
      properties:
        sessions: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "started_at": {"type": "string"}, "message_count": {"type": "integer"}}} }
      required: [sessions]
    SessionMessagesResponse:
      type: object
      description: api-spec 端点 /v1/sessions/{id}/messages 请求/响应 schema（D-428 收尾补全）
      properties:
        messages: { type: array, items: {"type": "object", "properties": {"role": {"type": "string"}, "content": {"type": "string"}, "created_at": {"type": "string"}}} }
      required: [messages]
    SessionMessageWriteRequest:
      type: object
      description: api-spec 端点 /v1/sessions/{id}/messages 请求/响应 schema（D-428 收尾补全）
      properties:
        role: { type: string, enum: ["user", "assistant", "system"] }
        content: { type: string }
      required: [role, content]
    SessionMessageWriteResponse:
      type: object
      description: api-spec 端点 /v1/sessions/{id}/messages 请求/响应 schema（D-428 收尾补全）
      properties:
        id: { type: string }
        created_at: { type: string }
      required: [id, created_at]
    EntityExtractRequest:
      type: object
      description: api-spec 端点 /v1/entities/extract 请求/响应 schema（D-428 收尾补全）
      properties:
        text: { type: string }
        user_id: { type: string }
      required: [text]
    EntityExtractResponse:
      type: object
      description: api-spec 端点 /v1/entities/extract 请求/响应 schema（D-428 收尾补全）
      properties:
        entities: { type: array, items: {"type": "object", "properties": {"name": {"type": "string"}, "type": {"type": "string"}, "description": {"type": "string"}}} }
        count: { type: integer }
      required: [entities, count]
    GraphSearchRequest:
      type: object
      description: api-spec 端点 /v1/graph/search 请求/响应 schema（D-428 收尾补全）
      properties:
        query: { type: string }
        user_id: { type: string }
        limit: { type: integer }
        relation_type: { type: string }
      required: [query]
    GraphSearchResponse:
      type: object
      description: api-spec 端点 /v1/graph/search 请求/响应 schema（D-428 收尾补全）
      properties:
        entities: { type: array, items: {"type": "object"} }
        relations: { type: array, items: {"type": "object"} }
        hops: { type: integer }
      required: [entities, relations, hops]
    MaintenanceRunRequest:
      type: object
      description: api-spec 端点 /v1/maintenance/run 请求/响应 schema（D-428 收尾补全）
      properties:
        mode: { type: string, enum: ["light", "deep"] }
        user_id: { type: string }
      required: [mode]
    MaintenanceRunResponse:
      type: object
      description: api-spec 端点 /v1/maintenance/run 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["started", "running", "completed"] }
      required: [status]
    MaintenanceStatusResponse:
      type: object
      description: api-spec 端点 /v1/maintenance/status 请求/响应 schema（D-428 收尾补全）
      properties:
        last_light_at: { type: [string, "null"] }
        last_deep_at: { type: [string, "null"] }
        total_merged: { type: integer }
        total_extracted_entities: { type: integer }
      required: [last_light_at, last_deep_at]
    ReflectRequest:
      type: object
      description: api-spec 端点 /v1/reflect 请求/响应 schema（D-428 收尾补全）
      properties:
        memory_ids: { type: array, items: {"type": "string"} }
        analysis_type: { type: string, enum: ["overview", "consistency", "mental_model"] }
      required: [memory_ids]
    ReflectResponse:
      type: object
      description: api-spec 端点 /v1/reflect 请求/响应 schema（D-428 收尾补全）
      properties:
        insights: { type: array, items: {"type": "object"} }
      required: [insights]
    HealthCalibrationResponse:
      type: object
      description: api-spec 端点 /v1/health/calibration 请求/响应 schema（D-428 收尾补全）
      properties:
        level: { type: string, enum: ["healthy", "degraded", "virtual", "dormant"] }
        last_external_calibration: { type: [string, "null"] }
        days_since_last_calibration: { type: integer }
        active_mode: { type: string }
        virtual_calibration_confidence_ceiling: { type: number }
      required: [level]
    HealthDetailResponse:
      type: object
      description: api-spec 端点 /v1/health/detail 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["ok", "degraded", "down"] }
        layers:
          type: object
          properties:
            access: { type: string }
            storage: { type: string }
            metacognition: { type: string }
      required: [status]
    MemoryPressureResponse:
      type: object
      description: api-spec 端点 /v1/health/memory-pressure 请求/响应 schema（D-428 收尾补全）
      properties:
        pressure: { type: number, minimum: 0, maximum: 1 }
        total_bytes: { type: integer }
      required: [pressure]
    PlaybookCreateRequest:
      type: object
      description: api-spec 端点 /v1/playbooks 请求/响应 schema（D-428 收尾补全）
      properties:
        name: { type: string }
        description: { type: string }
        steps: { type: array, items: {"type": "object"} }
      required: [name]
    PlaybookCreateResponse:
      type: object
      description: api-spec 端点 /v1/playbooks 请求/响应 schema（D-428 收尾补全）
      properties:
        id: { type: string }
        name: { type: string }
        status: { type: string, enum: ["active", "draft"] }
      required: [id, name]
    PlaybookListResponse:
      type: object
      description: api-spec 端点 /v1/playbooks 请求/响应 schema（D-428 收尾补全）
      properties:
        playbooks: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "name": {"type": "string"}, "status": {"type": "string"}}} }
      required: [playbooks]
    PlaybookSearchResponse:
      type: object
      description: api-spec 端点 /v1/playbooks/search 请求/响应 schema（D-428 收尾补全）
      properties:
        playbooks: { type: array, items: {"type": "object"} }
        total: { type: integer }
      required: [playbooks]
    PlaybookFeedbackRequest:
      type: object
      description: api-spec 端点 /v1/playbooks/{id}/feedback 请求/响应 schema（D-428 收尾补全）
      properties:
        feedback: { type: string, enum: ["helpful", "unhelpful", "incorrect"] }
        reason: { type: string }
      required: [feedback]
    PlaybookFeedbackResponse:
      type: object
      description: api-spec 端点 /v1/playbooks/{id}/feedback 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["recorded"] }
      required: [status]
    SearchExplainResponse:
      type: object
      description: api-spec 端点 /v1/search/explain 请求/响应 schema（D-428 收尾补全）
      properties:
        query: { type: string }
        signals:
          type: object
          properties:
            semantic: { type: number }
            bm25: { type: number }
            entity: { type: number }
      required: [query, signals]
    HallPromoteRequest:
      type: object
      description: api-spec 端点 /v1/halls/promote 请求/响应 schema（D-428 收尾补全）
      properties:
        memory_id: { type: string }
        reason: { type: string }
      required: [memory_id]
    HallPromoteResponse:
      type: object
      description: api-spec 端点 /v1/halls/promote 请求/响应 schema（D-428 收尾补全）
      properties:
        memory_id: { type: string }
        hall: { type: string, enum: ["processing", "validation", "canonical"] }
      required: [memory_id, hall]
    HallDemoteRequest:
      type: object
      description: api-spec 端点 /v1/halls/demote 请求/响应 schema（D-428 收尾补全）
      properties:
        memory_id: { type: string }
        reason: { type: string }
      required: [memory_id]
    HallDemoteResponse:
      type: object
      description: api-spec 端点 /v1/halls/demote 请求/响应 schema（D-428 收尾补全）
      properties:
        memory_id: { type: string }
        hall: { type: string, enum: ["processing", "validation", "canonical"] }
      required: [memory_id, hall]
    HallListResponse:
      type: object
      description: api-spec 端点 /v1/halls/{hall} 请求/响应 schema（D-428 收尾补全）
      properties:
        memories: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "hall": {"type": "string"}, "distill_level": {"type": "integer"}}} }
      required: [memories]
    SyncPushRequest:
      type: object
      description: api-spec 端点 /v1/sync/push 请求/响应 schema（D-428 收尾补全）
      properties:
        changes: { type: array, items: {"type": "object"} }
        device_id: { type: string }
      required: [changes]
    SyncPushResponse:
      type: object
      description: api-spec 端点 /v1/sync/push 请求/响应 schema（D-428 收尾补全）
      properties:
        accepted: { type: integer }
        conflicts: { type: array, items: {"type": "object"} }
      required: [accepted, conflicts]
    SyncPullRequest:
      type: object
      description: api-spec 端点 /v1/sync/pull 请求/响应 schema（D-428 收尾补全）
      properties:
        since: { type: string }
        limit: { type: integer }
      required: [since]
    SyncPullResponse:
      type: object
      description: api-spec 端点 /v1/sync/pull 请求/响应 schema（D-428 收尾补全）
      properties:
        changes: { type: array, items: {"type": "object"} }
        next_cursor: { type: [string, "null"] }
      required: [changes, next_cursor]
    SyncExportRequest:
      type: object
      description: api-spec 端点 /v1/sync/export 请求/响应 schema（D-428 收尾补全）
      properties:
        format: { type: string, enum: ["json", "markdown"] }
        include_metadata: { type: boolean }
      required: [format]
    SyncExportResponse:
      type: object
      description: api-spec 端点 /v1/sync/export 请求/响应 schema（D-428 收尾补全）
      properties:
        archive_id: { type: string }
        download_url: { type: string }
      required: [archive_id]
    SyncImportRequest:
      type: object
      description: api-spec 端点 /v1/sync/import 请求/响应 schema（D-428 收尾补全）
      properties:
        archive_id: { type: string }
        conflict_resolution: { type: string, enum: ["fail", "overwrite", "skip"] }
      required: [archive_id]
    SyncImportResponse:
      type: object
      description: api-spec 端点 /v1/sync/import 请求/响应 schema（D-428 收尾补全）
      properties:
        imported: { type: integer }
        skipped: { type: integer }
        failed: { type: integer }
      required: [imported, skipped, failed]
    NarrativeThreadCreateRequest:
      type: object
      description: api-spec 端点 /v1/narrative/threads 请求/响应 schema（D-428 收尾补全）
      properties:
        title: { type: string }
        description: { type: string }
        member_ids: { type: array, items: {"type": "string"} }
      required: [title]
    NarrativeThreadCreateResponse:
      type: object
      description: api-spec 端点 /v1/narrative/threads 请求/响应 schema（D-428 收尾补全）
      properties:
        id: { type: string }
        title: { type: string }
        status: { type: string, enum: ["active", "completed"] }
      required: [id, title]
    NarrativeThreadListResponse:
      type: object
      description: api-spec 端点 /v1/narrative/threads 请求/响应 schema（D-428 收尾补全）
      properties:
        threads: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "title": {"type": "string"}, "status": {"type": "string"}}} }
      required: [threads]
    NarrativeThreadMembersRequest:
      type: object
      description: api-spec 端点 /v1/narrative/threads/{id}/members 请求/响应 schema（D-428 收尾补全）
      properties:
        member_ids: { type: array, items: {"type": "string"} }
      required: [member_ids]
    NarrativeThreadMembersResponse:
      type: object
      description: api-spec 端点 /v1/narrative/threads/{id}/members 请求/响应 schema（D-428 收尾补全）
      properties:
        added: { type: integer }
        members: { type: integer }
      required: [added]
    NarrativeThreadMemoriesResponse:
      type: object
      description: api-spec 端点 /v1/narrative/threads/{id}/memories 请求/响应 schema（D-428 收尾补全）
      properties:
        memories: { type: array, items: {"type": "object"} }
        total: { type: integer }
      required: [memories]
    NarrativeThreadSummarizeRequest:
      type: object
      description: api-spec 端点 /v1/narrative/threads/{id}/summarize 请求/响应 schema（D-428 收尾补全）
      properties:
        level: { type: string, enum: ["summary", "overview", "full"] }
      required: [level]
    NarrativeThreadSummarizeResponse:
      type: object
      description: api-spec 端点 /v1/narrative/threads/{id}/summarize 请求/响应 schema（D-428 收尾补全）
      properties:
        summary: { type: string }
      required: [summary]
    NarrativeThreadCompleteResponse:
      type: object
      description: api-spec 端点 /v1/narrative/threads/{id}/complete 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["completed"] }
      required: [status]
    CompactionRunRequest:
      type: object
      description: api-spec 端点 /v1/admin/compaction/run 请求/响应 schema（D-428 收尾补全）
      properties:
        path: { type: string }
        mode: { type: string, enum: ["full", "incremental"] }
    CompactionRunResponse:
      type: object
      description: api-spec 端点 /v1/admin/compaction/run 请求/响应 schema（D-428 收尾补全）
      properties:
        snapshot_id: { type: string }
        status: { type: string, enum: ["completed", "running"] }
      required: [snapshot_id]
    CompactionRollbackRequest:
      type: object
      description: api-spec 端点 /v1/admin/compaction/rollback/{snapshot_id} 请求/响应 schema（D-428 收尾补全）
      properties:
        reason: { type: string }
    CompactionRollbackResponse:
      type: object
      description: api-spec 端点 /v1/admin/compaction/rollback/{snapshot_id} 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["rolled_back"] }
        restored_memories: { type: integer }
      required: [status]
    CausalLinkRequest:
      type: object
      description: api-spec 端点 /v1/causal 请求/响应 schema（D-428 收尾补全）
      properties:
        from_id: { type: string }
        to_id: { type: string }
        relation_type: { type: string, enum: ["causal", "temporal", "correlational"] }
        strength: { type: number, minimum: 0, maximum: 1 }
      required: [from_id, to_id]
    CausalLinkResponse:
      type: object
      description: api-spec 端点 /v1/causal 请求/响应 schema（D-428 收尾补全）
      properties:
        link_id: { type: string }
        status: { type: string, enum: ["created"] }
      required: [link_id, status]
    FactWriteRequest:
      type: object
      description: api-spec 端点 /v1/facts 请求/响应 schema（D-428 收尾补全）
      properties:
        subject: { type: string }
        predicate: { type: string }
        object: { type: string }
        confidence: { type: number, minimum: 0, maximum: 1 }
      required: [subject, predicate, object]
    FactWriteResponse:
      type: object
      description: api-spec 端点 /v1/facts 请求/响应 schema（D-428 收尾补全）
      properties:
        fact_id: { type: string }
        status: { type: string, enum: ["created"] }
      required: [fact_id, status]
    EdgeTypeListResponse:
      type: object
      description: api-spec 端点 /v1/edge-types 请求/响应 schema（D-428 收尾补全）
      properties:
        edge_types: { type: array, items: {"type": "object", "properties": {"name": {"type": "string"}, "legal_combinations": {"type": "array", "items": {"type": "string"}}}} }
      required: [edge_types]
    EdgeTypeRegisterRequest:
      type: object
      description: api-spec 端点 /v1/edge-types 请求/响应 schema（D-428 收尾补全）
      properties:
        name: { type: string }
        signature: { type: object }
      required: [name, signature]
    EdgeTypeRegisterResponse:
      type: object
      description: api-spec 端点 /v1/edge-types 请求/响应 schema（D-428 收尾补全）
      properties:
        name: { type: string }
        status: { type: string, enum: ["registered"] }
      required: [name, status]
    EdgeTypeUpdateRequest:
      type: object
      description: api-spec 端点 /v1/edge-types/{type} 请求/响应 schema（D-428 收尾补全）
      properties:
        legal_combinations: { type: array, items: {"type": "string"} }
      required: [legal_combinations]
    EdgeTypeUpdateResponse:
      type: object
      description: api-spec 端点 /v1/edge-types/{type} 请求/响应 schema（D-428 收尾补全）
      properties:
        name: { type: string }
        status: { type: string, enum: ["updated"] }
      required: [name, status]
    SkillListResponse:
      type: object
      description: api-spec 端点 /v1/skills 请求/响应 schema（D-428 收尾补全）
      properties:
        skills: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "name": {"type": "string"}, "level": {"type": "integer"}}} }
      required: [skills]
    SkillSupersedeRequest:
      type: object
      description: api-spec 端点 /v1/skills/{id}/supersede 请求/响应 schema（D-428 收尾补全）
      properties:
        replacement_id: { type: string }
        reason: { type: string }
      required: [replacement_id]
    SkillSupersedeResponse:
      type: object
      description: api-spec 端点 /v1/skills/{id}/supersede 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["superseded"] }
      required: [status]
    SkillReactivateRequest:
      type: object
      description: api-spec 端点 /v1/skills/{id}/reactivate 请求/响应 schema（D-428 收尾补全）
      properties:
        reason: { type: string }
    SkillReactivateResponse:
      type: object
      description: api-spec 端点 /v1/skills/{id}/reactivate 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["active"] }
      required: [status]
    ConnectorRegisterRequest:
      type: object
      description: api-spec 端点 /v1/connectors/register 请求/响应 schema（D-428 收尾补全）
      properties:
        platform: { type: string, enum: ["gmail", "drive", "notion", "github"] }
        credentials: { type: object }
      required: [platform, credentials]
    ConnectorRegisterResponse:
      type: object
      description: api-spec 端点 /v1/connectors/register 请求/响应 schema（D-428 收尾补全）
      properties:
        connector_id: { type: string }
        status: { type: string, enum: ["active"] }
      required: [connector_id, status]
    ProfileSchemaRequest:
      type: object
      description: api-spec 端点 /v1/profile/schema 请求/响应 schema（D-428 收尾补全）
      properties:
        schema: { type: object }
      required: [schema]
    ProfileSchemaResponse:
      type: object
      description: api-spec 端点 /v1/profile/schema 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["updated"] }
      required: [status]
    AdminExportRequest:
      type: object
      description: api-spec 端点 /v1/admin/export 请求/响应 schema（D-428 收尾补全）
      properties:
        format: { type: string, enum: ["kairos", "json"] }
    AdminExportResponse:
      type: object
      description: api-spec 端点 /v1/admin/export 请求/响应 schema（D-428 收尾补全）
      properties:
        archive_id: { type: string }
        download_url: { type: string }
      required: [archive_id]
    AdminImportRequest:
      type: object
      description: api-spec 端点 /v1/admin/import 请求/响应 schema（D-428 收尾补全）
      properties:
        archive_id: { type: string }
        conflict_resolution: { type: string, enum: ["fail", "overwrite", "skip"] }
      required: [archive_id]
    AdminImportResponse:
      type: object
      description: api-spec 端点 /v1/admin/import 请求/响应 schema（D-428 收尾补全）
      properties:
        imported: { type: integer }
        skipped: { type: integer }
        failed: { type: integer }
      required: [imported, skipped, failed]
    ConsistencyCheckResponse:
      type: object
      description: api-spec 端点 /v1/admin/consistency-check 请求/响应 schema（D-428 收尾补全）
      properties:
        checks: { type: array, items: {"type": "object", "properties": {"name": {"type": "string"}, "status": {"type": "string", "enum": ["ok", "failed"]}, "details": {"type": "string"}}} }
      required: [checks]
    GraphRenderResponse:
      type: object
      description: api-spec 端点 /v1/graph/render 请求/响应 schema（D-428 收尾补全）
      properties:
        graph:
          type: object
          properties:
            nodes: { type: array, items: {"type": "object"} }
            edges: { type: array, items: {"type": "object"} }
      required: [graph]
    ConstitutionRequest:
      type: object
      description: api-spec 端点 /v1/constitution 请求/响应 schema（D-428 收尾补全）
      properties:
        action: { type: string, enum: ["view", "revise", "contract_downgrade", "state_restore"] }
        preference_key: { type: string }
        new_value: { type: string }
        memory_id: { type: string }
        case_id: { type: string }
      required: [action]
    ConstitutionResponse:
      type: object
      description: api-spec 端点 /v1/constitution 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["applied", "viewed"] }
        result: { type: object }
      required: [status]
    PathRebuildResponse:
      type: object
      description: api-spec 端点 /v1/path/rebuild-index 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["rebuilt"] }
        paths_rebuilt: { type: integer }
      required: [status]
    PathSuppressRequest:
      type: object
      description: api-spec 端点 /v1/path/suppress 请求/响应 schema（D-428 收尾补全）
      properties:
        path_prefix: { type: string }
        reason: { type: string }
      required: [path_prefix, reason]
    PathSuppressResponse:
      type: object
      description: api-spec 端点 /v1/path/suppress 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["suppressed"] }
      required: [status]
    HeatTopResponse:
      type: object
      description: api-spec 端点 /v1/memories/heat-top 请求/响应 schema（D-428 收尾补全）
      properties:
        data: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "content": {"type": "string"}, "heat_score": {"type": "number"}}} }
        total: { type: integer }
      required: [data, total]
    MemoryStatsResponse:
      type: object
      description: api-spec 端点 /v1/memories/stats 请求/响应 schema（D-428 收尾补全）
      properties:
        total: { type: integer }
        by_type: { type: object }
        by_state: { type: object }
        growth_7d: { type: object }
      required: [total]
    MemoryMergeRequest:
      type: object
      description: api-spec 端点 /v1/memories/merge 请求/响应 schema（D-428 收尾补全）
      properties:
        source_ids: { type: array, items: {"type": "string"} }
        strategy: { type: string, enum: ["semantic_overlay", "chronological_append"] }
      required: [source_ids, strategy]
    MemoryMergeResponse:
      type: object
      description: api-spec 端点 /v1/memories/merge 请求/响应 schema（D-428 收尾补全）
      properties:
        merged_id: { type: string }
        sources: { type: array, items: {"type": "string"} }
        status: { type: string, enum: ["merged"] }
      required: [merged_id, sources, status]
    MemoryExpireRequest:
      type: object
      description: api-spec 端点 /v1/memories/{id}/expire 请求/响应 schema（D-428 收尾补全）
      properties:
        ttl_seconds: { type: integer, minimum: 1 }
      required: [ttl_seconds]
    MemoryExpireResponse:
      type: object
      description: api-spec 端点 /v1/memories/{id}/expire 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["expiring"] }
        expires_at: { type: string }
      required: [status]
    MemoryExportResponse:
      type: object
      description: api-spec 端点 /v1/memories/{id}/export 请求/响应 schema（D-428 收尾补全）
      properties:
        id: { type: string }
        content: { type: string }
        format: { type: string, enum: ["json", "markdown"] }
      required: [id, content]
    MemoryFeedbackRequest:
      type: object
      description: api-spec 端点 /v1/memories/{id}/feedback 请求/响应 schema（D-428 收尾补全）
      properties:
        feedback: { type: string, enum: ["helpful", "unhelpful", "incorrect"] }
        reason: { type: string }
      required: [feedback]
    MemoryFeedbackResponse:
      type: object
      description: api-spec 端点 /v1/memories/{id}/feedback 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["recorded"] }
      required: [status]
    MemoryLockRequest:
      type: object
      description: api-spec 端点 /v1/memories/{id}/lock 请求/响应 schema（D-428 收尾补全）
      properties:
        reason: { type: string }
        duration_seconds: { type: integer, minimum: 1 }
      required: [reason, duration_seconds]
    MemoryLockResponse:
      type: object
      description: api-spec 端点 /v1/memories/{id}/lock 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["locked"] }
        locked_until: { type: string }
      required: [status]
    MemoryRollbackRequest:
      type: object
      description: api-spec 端点 /v1/memories/{id}/rollback 请求/响应 schema（D-428 收尾补全）
      properties:
        target_version: { type: integer, minimum: 1 }
      required: [target_version]
    MemoryRollbackResponse:
      type: object
      description: api-spec 端点 /v1/memories/{id}/rollback 请求/响应 schema（D-428 收尾补全）
      properties:
        version: { type: integer }
        status: { type: string, enum: ["rolled_back"] }
      required: [version]
    MemorySuppressRequest:
      type: object
      description: api-spec 端点 /v1/memories/{id}/suppress 请求/响应 schema（D-428 收尾补全）
      properties:
        reason: { type: string, enum: ["compliance_erase"] }
        review_id: { type: string }
      required: [reason]
    MemorySuppressResponse:
      type: object
      description: api-spec 端点 /v1/memories/{id}/suppress 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["suppressed"] }
      required: [status]
    MemoryVersionsResponse:
      type: object
      description: api-spec 端点 /v1/memories/{id}/versions 请求/响应 schema（D-428 收尾补全）
      properties:
        versions: { type: array, items: {"type": "object", "properties": {"version_number": {"type": "integer"}, "created_at": {"type": "string"}, "reason": {"type": "string"}}} }
      required: [versions]
    ProactiveTopicsResponse:
      type: object
      description: api-spec 端点 /v1/proactive/topics 请求/响应 schema（D-428 收尾补全）
      properties:
        topics: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "topic": {"type": "string"}, "priority": {"type": "number"}, "status": {"type": "string"}}} }
        total: { type: integer }
      required: [topics, total]
    SchedulerStatusResponse:
      type: object
      description: api-spec 端点 /v1/scheduler/status 请求/响应 schema（D-428 收尾补全）
      properties:
        jobs: { type: array, items: {"type": "object", "properties": {"id": {"type": "string"}, "next_run": {"type": ["string", "null"]}}} }
      required: [jobs]
    FalsificationResponse:
      type: object
      description: api-spec 端点 /v1/falsification 请求/响应 schema（D-428 收尾补全）
      properties:
        signals: { type: array, items: {"type": "object", "properties": {"hypothesis": {"type": "string"}, "survival": {"type": "string", "enum": ["true", "false", "challenged"]}, "evidence_count": {"type": "integer"}}} }
      required: [signals]
    EvolutionResponse:
      type: object
      description: api-spec 端点 /v1/evolution/{id} 请求/响应 schema（D-428 收尾补全）
      properties:
        id: { type: string }
        evolution: { type: array, items: {"type": "object"} }
      required: [id, evolution]
    WebhookRegisterRequest:
      type: object
      description: api-spec 端点 /v1/webhooks 请求/响应 schema（D-428 收尾补全）
      properties:
        url: { type: string }
        events: { type: array, items: {"type": "string"} }
        secret: { type: string }
      required: [url, events]
    WebhookRegisterResponse:
      type: object
      description: api-spec 端点 /v1/webhooks 请求/响应 schema（D-428 收尾补全）
      properties:
        id: { type: string }
        status: { type: string, enum: ["active"] }
      required: [id, status]
    WebhookListResponse:
      type: object
      description: api-spec 端点 /v1/webhooks 请求/响应 schema（D-428 收尾补全）
      properties:
        webhooks: { type: array, items: {"type": "object"} }
      required: [webhooks]
    WebhookDeleteResponse:
      type: object
      description: api-spec 端点 /v1/webhooks 请求/响应 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ["deleted"] }
      required: [status]
    CompressionAuditResponse:
      type: object
      description: api-spec 端点 /audit/compression 请求/响应 schema（D-428 收尾补全）
      properties:
        records: { type: array, items: {"type": "object", "properties": {"memory_id": {"type": "string"}, "compacted_at": {"type": "string"}, "trail": {"type": "object"}}} }
      required: [records]
    CompressionSummaryResponse:
      type: object
      description: api-spec 端点 /audit/compression/summary 请求/响应 schema（D-428 收尾补全）
      properties:
        total: { type: integer }
        window_days: { type: integer }
        by_reason: { type: object }
      required: [total, window_days]
    DegradationSwitchResponse:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        mode: { type: string, enum: ['conservative_silent', 'limited_cross_validation', 'safe_hibernation', 'normal'] }
        previous_mode: { type: string }
        status: { type: string, enum: ['switched'] }
    FreezeResponse:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ['frozen'] }
        frozen_until: { type: string }
    UnfreezeRequest:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        note: { type: string, description: 无参数 }
    UnfreezeResponse:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ['unfrozen'] }
    MemoryDeleteResponse:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        status: { type: string, enum: ['deleted'] }
        id: { type: string }
    MemoryArchiveResponse:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        memory_id: { type: string }
        status: { type: string, enum: ['archived'] }
        idempotent: { type: boolean }
    MemoryRestoreResponse:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        memory_id: { type: string }
        status: { type: string, enum: ['active'] }
        previous_state: { type: string }
    NarrativeThreadCompleteRequest:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        reason: { type: string }
    PathRebuildRequest:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        path: { type: string }
    ErrorResponse:
      type: object
      description: api-spec 端点 schema（D-428 收尾补全）
      properties:
        code: { type: string }
        message: { type: string }
        details: { type: object }
    ConsistencyCheckRequest:
      type: object
      description: api-spec 端点 /v1/admin/consistency-check 请求 schema（D-428 收尾补全）
      properties:
        note: { type: string, description: 无参数 }
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: 'API Key（read / write / admin 三级），经 `Authorization: Bearer <key>` 传递（见 api-spec.md 引言「认证」）。所需角色由各操作的 x-kairos-required-role 标注。'
security:
- bearerAuth: []