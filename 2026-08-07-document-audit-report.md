# Kairos-Memory 文档库深度审计报告

> 审计对象：`D:\projects\kairos-memory`（文档设计阶段，无运行代码，168 能力架构就绪、核心引擎未启动）
> 审计基线：changelog 0.0.42（2026-08-07）；门禁脚本 `doc-audit.py` 18 类 + 14a + 6.x 子检查
> 审计范围：全库 `docs/`（`scripts/` 仅审计脚本本体，不在文档审计范围）
> 审计日期：2026-08-07

---

## 模块一 · 审计概览

### 1.1 整体评价

Kairos-Memory 文档库已具备**进入开发阶段的条件（放行）**。支撑这一结论的事实：

- **门禁全绿**：`doc-audit.py` 18 类 + 14a + 6.13~6.16 + 6.17/6.18/6.19 全部通过，退出码 0；配置文件索引 370/370、债务闭环 13/13、元数据日期 183 份一致、编号连续性、废弃术语、锚点链接（仅 1 处待修）均达标。
- **核心计数口径经验证全部属实**：168 功能（43 核心 + 125 扩展）、57 物理表、370 配置参数（224+146）、69 术语、38 错误码、66 操作（OP-001~066）、37 声明（C-01~C-37，活跃 36）、70 组件、88 物理端点（85 `/v1` + 3 非前缀）、12 ADR、9 E2E。本轮逐一重算复核，与权威文档一致。
- **历史高/中风险项已收敛**：round14 提出的 3 项【中】（治理面计数、ADM→CS 指令措辞、检索权重双套）本轮复核均已闭环——尤其 R14-03 检索权重双套口径，旧 `0.55/0.30/0.15` 已在正文显式标注为「被 Link-3 四链路 `0.50/0.20/0.10/0.20` 取代的历史配比」，`KAIROS_RETRIEVAL_LINK_WEIGHTS` 已定义，且「三信号（0.50/0.35/0.15，权重和恒为 1）」与「四链路（0.50/0.20/0.10/0.20，权重和恒为 1）」分属候选域重排序与候选扩展两个不同层级，无事实矛盾。
- **文档治理纪律成熟**：零版本标记、术语单义、数字可实证、写了=做了、追缴门禁等元规则已在 `governance/documentation-governance.md` 形成可执行约束，并通过 gate 自动化卡点落地。

### 1.2 成熟度结论

**成熟度 = 放行（准开发就绪）。风险分布：0【高】 / 5【中】 / 4【低】。**

无【高】阻断项。5 项【中】均为治理纪律与接口清晰度问题（不影响设计事实正确性），建议在代码启动前或首迭代内闭环；4 项【低】为规范/组织体验问题，可排入常规文档维护。

**进入开发前建议优先闭环的【中】项**：F2（MCP/SDK 工具数口径矛盾）、F3（声明编号 C/AP 双轨、claim-matrix 未同步）、F5（无机器可读接口契约），这三项直接关系开发者对接口面与声明事实源的理解。

---

## 模块二 · 问题清单（按 5 个维度分类）

### 维度 1 · 完整性与一致性

**F1【中】架构文档 23 处「外部理念吸收，changelog 0.0.40」标注批次号错标 + 格式违规**
- 架构 `foundation/architecture-v0.1.0.md` 中存在 23 处形如 `**（外部理念吸收，changelog 0.0.40）**` 的注记（如 L647、L1463、L1496、L1905、L1993、L2065、L2120、L2330、L2472、L2480、L2507、L2988、L3287、L3350 等）。
- **批次号错标**：changelog 显示 0.0.40 为「外部视频分析批次（100 视频 + 15 仓库）」，而 AP-01~28 的「外部理念吸收落地批次」实为 **0.0.41**（认知基础 8 条声明 + 架构 22 条机制 + 规格层 7 份文档）。这些机制落地注记应指向 0.0.41，而非 0.0.40。
- **格式违规**：`documentation-governance.md` §6 零版本标记纪律禁止正文段落/表格注释承载「changelog X.X.XX」版本批次标记；0.0.42 修订的豁免 2 仅允许「实证参考基线」标记（参数/阈值/校准类）或「（外部实证参考：代号）」简注（机制类）。当前 `外部理念吸收，changelog 0.0.40` 两轨皆非，属不合规标注。
- 注：部分注记末尾已带合规的「外部实证：RF-Mem（VID-58）」等来源简注，说明正文机制描述本身已满足豁免 2 机制类要求，仅前端 `外部理念吸收，changelog 0.0.40` 标签需清除/改写。

**F2【中】technology-stack.md「12 规范操作集」与「15 个工具」术语矛盾**
- `development/technology-stack.md` 第 95 行与第 114 行仍使用旧术语「12 规范操作集」描述 MCP/SDK 暴露的接口面。
- 但同文档第 98 行（仅隔 3 行）已按 0.0.28 修正写为「完整工具清单（15 个：基础工具集 12——含 3 规范操作直接映射 + 检索/维护/治理类 9 个——另加关系管理 3 工具，构成口径见架构 §7.1a）」。
- 即同一文档内「12 规范操作集」（L95/L114）与「15 个工具」（L98）并存，开发者无法判定 MCP/SDK 对外接口是 12 还是 15。0.0.28 已修正构成公式但未清扫这两处残留实例（whack-a-mole 遗漏）。

**F3【中】声明编号 C/AP 双轨，claim-implementation-matrix 单一事实源未同步 0.0.41**
- 0.0.41 引入 AP-01~28 编号体系，其中 8 条认知层声明（AP-01/02/03/05/06/19/22/28）已落地 `cognitive-foundation.md`（其版本记录 L1176 明确记载）。
- 但 `specification/claim-implementation-matrix.md`（声明承载矩阵，§6 单一事实源）仍为 C-01~C-37、共 47 表行，无任何 AP- 条目、未提及 0.0.41 批次。
- 结果：声明编号出现 C/AP 双轨，且全库对外宣称的「37 声明」口径与 0.0.41 新增 8 条 AP 声明的关系未澄清——读者无法从单一事实源确认当前声明总数与承载状态。

**F4【中】4 处「参数待登记」阈值未进 debt-collection 追缴台账，亦未登记 configuration.md**
- 架构中存在 4 处引用了阈值但标注「参数待登记」：候选集集中度信号阈值（L1496）、升华整合门控证据充足性阈值（L1993）、处理完降温衰减系数（L2472）、间隔重复复习间隔序列（L3388）。
- 配置参数总数仍为 370（changelog 0.0.41 明确「参数 370 不变（4 处新阈值标注『参数待登记』）」），即这 4 个阈值尚未在 `ops/configuration.md` 注册为 `KAIROS_*` 参数。
- `governance/debt-collection.md` 中 AP- 条目数 = 0，亦无「参数待登记」类追缴条目。按文档纪律 §2.3「追缴门禁」：任何「后续/未来版本」或推迟登记项必须同步创建 debt-collection 条目（到期日/改动量/不做的后果/状态）。当前 4 处「待登记」仅靠正文内联标注，未进追缴台账，违反追缴门禁。

### 维度 2 · 缺失与过时

**F5【中】无机器可读接口契约（OpenAPI 3.x / JSON Schema）**
- 88 个物理端点（85 `/v1` + 3 非前缀）与 15 个 MCP 工具均仅以 Markdown（`specification/api-spec.md` + `integration-design.md`）和 1 份 HTML 架构图描述，全库无 `.json` / `openapi*` / `.yaml` 契约文件（`find docs -iname "*.json" -o -iname "openapi*" -o -iname "*.yaml"` 返回空）。
- 影响：开发阶段无法自动生成客户端 SDK 骨架、请求/响应校验器、Mock Server，Markdown 与实现易漂移；对 88 端点量级尤甚。属「缺失关键开发就绪产物」。

**F6【低】technology-stack.md 元数据日期与版本记录滞后**
- `technology-stack.md` frontmatter `updated: 2026-08-06`，版本记录止于 0.0.38（round16）。全库最新活动为 2026-08-07 / 0.0.42。
- 0.0.39（外部理念吸收 6 项）、0.0.40（视频分析）、0.0.41（AP-01~28 落库）、0.0.42（审计修复）均未在本文档留痕。虽 0.0.39~0.0.41 多为设计机制吸收、未必改技术选型，但缺乏「本批无技术选型变更」的确认注记，且 `updated` 日期与全库不一致，属元数据卫生问题。

**F7【低】data-model.md 零内联 DDL 且未显式声明政策**
- `specification/data-model.md` 本身不含任何 `CREATE TABLE`，57 张物理表的 DDL 全在 `specification/schema-slice.sql`（14 张竖切示例 DDL）。此为有意政策（数据模型文档描述结构，DDL 集中管理）。
- 但 `data-model.md` 正文未显式声明「本文档不含 DDL，DDL 见 schema-slice.sql」，新读者易误判为缺表定义或查找遗漏。

### 维度 3 · 结构与组织

**F8【低】分析产物目录边界与 README 计数口径未明确**
- `docs/analysis/external-videos/` 含 131 份分析文档（102 视频笔记 + 15 仓库 + 9 论文 + 4 索引 + 1 流程），体量已远超多数核心设计文档，与 `docs/` 10 个权威内容目录并列。
- `docs/README.md` 索引口径仍为「184 md + 2 yaml」，门禁也仅扫描 184 份，说明 external-videos 被有意排除在权威口径外。但该排除边界（是否随仓库分发、是否纳入门禁、索引是否单列）未在结构层面文档化，存在边界模糊风险。

**F9【低】架构文档单文件 3952 行远超 200 行软约束**
- 治理 §4.2 规定新文档 ≤200 行，存量超限为历史形成、已豁免追溯拆分。但单文件承载 23 处外部理念吸收注记 + 22 条机制 + 全量规格，随内容增长需持续维护导航与锚点可达性。门禁 [15/18] 锚点链接检查已发现 1 处断链，提示超限文档的链接维护成本递增。

### 维度 4 · 格式规范

**F10【中/格式维度，与 F1 同源】正文版本标记违反 §6 零版本标记纪律**
- 即 F1 中的「外部理念吸收，changelog 0.0.40」标注，在格式规范维度同样构成违规：正文段落承载了 changelog 版本批次标记，与 §6 单一事实源原则及 0.0.42 豁免 2 规定格式不符。需在统一整改中改为豁免 2 双轨格式（`（外部实证参考：代号）` 或 `实证参考基线`）。
- 其余格式项（术语单义、编号连续性、交叉引用链接格式）经门禁与本轮抽查均达标，无新增违规。

### 维度 5 · 核心文档准确性

**F11【结论：通过，无缺陷】核心文档深度验证**
- 认知基础（`cognitive-foundation.md`）、架构设计（`architecture-v0.1.0.md`）、技术选型（`technology-stack.md`）三份核心文档经深度交叉验证：
  - 检索权重双套口径（历史 R14-03）已闭环（见 1.1）。
  - 三信号融合（语义 0.50 / BM25 0.35 / 实体 0.15，和=1）与四链路扩展（语义 0.50 / 共现 0.20 / kNN 0.10 / 因果 0.20，和=1）分属不同层级且无矛盾。
  - 全部核心计数口径与权威文档一致（见 1.1 清单）。
  - 认知基础「去版本化、纯认知理论」定位与架构「版本差距唯一载体」定位分层清晰，无混层。
- **维度 5 无【高】/【中】/【低】缺陷项**，核心设计事实正确性与内部一致性达到开发就绪标准。

---

## 模块三 · 风险等级汇总

| 编号 | 维度 | 问题 | 风险 |
|------|------|------|------|
| F1 | 1 完整性/一致性 | 架构 23 处「外部理念吸收，changelog 0.0.40」批次号错标（应为 0.0.41）+ 格式违规 | 【中】 |
| F2 | 1 完整性/一致性 | technology-stack「12 规范操作集」vs「15 个工具」术语矛盾 | 【中】 |
| F3 | 1 完整性/一致性 | 声明编号 C/AP 双轨，claim-matrix 单一事实源未同步 0.0.41 | 【中】 |
| F4 | 1 完整性/一致性 | 4 处「参数待登记」未进 debt-collection 追缴台账 | 【中】 |
| F5 | 2 缺失/过时 | 无机器可读接口契约（OpenAPI/JSON Schema） | 【中】 |
| F6 | 2 缺失/过时 | technology-stack 元数据日期/版本记录滞后（0.0.38 vs 0.0.42） | 【低】 |
| F7 | 2 缺失/过时 | data-model 零内联 DDL 未显式声明政策 | 【低】 |
| F8 | 3 结构/组织 | external-videos 分析目录边界与 README 计数口径未文档化 | 【低】 |
| F9 | 3 结构/组织 | 架构单文件 3952 行超限，导航/锚点维护成本递增 | 【低】 |
| F10 | 4 格式规范 | 正文版本标记违反 §6 零版本标记纪律（与 F1 同源） | 【中】 |
| F11 | 5 核心准确性 | 核心文档深度验证 | 【高】不阻断 / 通过 |

**合计：0【高】 / 5【中】（F1/F2/F3/F4/F5，F10 与 F1 同源计 1 处实质整改）/ 4【低】。**

---

## 模块四 · 改进建议（具体可落地）

### 针对【中】项（建议代码启动前闭环）

**A. 整改 F1/F10（架构标注合规化）**
- 全库批量检索并改写 `architecture-v0.1.0.md` 中 23 处 `**（外部理念吸收，changelog 0.0.40）**`：
  - 删除前端「外部理念吸收，changelog 0.0.40」版本批次标签；
  - 机制描述类保留/补全文末「（外部实证参考：VID-XX）」简注（符合豁免 2 机制类）；
  - 参数/阈值类（如候选集集中度阈值）改用「实证参考基线」标记（来源以代号简注）。
- 将批次归属由 0.0.40 更正为 **0.0.41**（AP-01~28 落库批次）。
- 改后运行 `python scripts/doc-audit.py` 确认门禁仍绿，并 grep 全库确认无残留 `外部理念吸收，changelog` 字样。
- 建议将「外部理念吸收注记格式」写入 `documentation-governance.md` §6 豁免 2 的示例，防止复发。

**B. 整改 F2（MCP/SDK 工具数口径统一）**
- `technology-stack.md` L95、L114 的「12 规范操作集」改为与 L98 一致的「基础工具集 12（含 3 规范操作直接映射）+ 关系管理 3 = 15 个 MCP Tool」表述；或统一术语为「15 个 MCP Tool（基础工具集 12 + 关系管理 3）」。
- 同步核查 `api-spec.md §6.8`、`architecture §7.1a`、`integration-design.md §七` 中工具计数表述是否均为 15，消除「12 vs 15」歧义。

**C. 整改 F3（声明单一事实源同步）**
- 在 `claim-implementation-matrix.md` 增补 AP-01~28 落地条目（或明确 AP 与 C 的映射关系），至少登记已落地的 8 条认知层声明（AP-01/02/03/05/06/19/22/28）及其承载文档/状态。
- 澄清全库「37 声明」口径：说明 C-01~C-37 为原声明体系，AP-xx 为外部理念吸收新增声明，二者是否合并计数、总数如何对外宣称，需在 `claim-implementation-matrix.md` 头部或 `README.md` 计数表一处定调。

**D. 整改 F4（追缴门禁补全）**
- 为 4 处「参数待登记」阈值在 `debt-collection.md` 创建追缴条目（建议 D-422~D-425 或沿用现有段末编号），含：到期日（建议 v0.1.0 配置冻结前）、改动量（在 `configuration.md` 注册 `KAIROS_*` 参数 + 默认值）、不做的后果（阈值硬编码于架构、失去可调性与运行时一致性）。
- 或在 0.0.43 批次直接将 4 个阈值注册进 `configuration.md`（更优——消除「待登记」状态），并同步门禁 [7/18] 配置参数索引。

**E. 补强 F5（机器可读契约，开发就绪）**
- 建议新增 `specification/api-contract/openapi.yaml`（OpenAPI 3.1），覆盖 85 个 `/v1` 业务端点 + 3 个非前缀端点；MCP 工具以 `mcp-tools.json` 描述 15 个 Tool 的 input/output schema。
- 若开发阶段首迭代才能产出，至少先建骨架 + 端点清单表，并在 `project-plan.md` 将其列为 Phase 0 交付物。该产物同时可作为 client SDK 代码生成的单一事实源，降低 Markdown 漂移。

### 针对【低】项（常规维护）

- **F6**：`technology-stack.md` 补 0.0.39~0.0.42 版本记录占位行（注明「本批无技术选型变更」），并将 `updated` 更新至 2026-08-07。
- **F7**：`data-model.md` 开头加一句「本文档描述逻辑数据模型，DDL 见 `schema-slice.sql`」，并链接。
- **F8**：在 `docs/README.md` 或 `AGENTS.md` 明确 `docs/analysis/` 为分析产物目录、不计入权威文档计数、不纳入门禁、随仓库分发策略；README 计数口径注明「不含 analysis/」。
- **F9**：为 `architecture-v0.1.0.md` 维护一份节级导航索引（或拆分超长章节为子文档），并定期 grep 锚点断链（门禁 [15/18] 已覆盖，保持即可）。

### 门禁增强建议（预防复发）
- 在 `doc-audit.py` 新增子检查：① 正文 `changelog X.X.XX` 版本批次标记扫描（捕获 F1/F10 类）；② `claim-implementation-matrix.md` 与 cognitive-foundation 版本记录中 AP-/C- 编号一致性；③ configuration 参数索引与架构「参数待登记」标注反向对账。

---

## 附：本次审计已确认的「无问题」基线（避免重复审计）

- 门禁 18 类 + 14a + 6.13~6.19 全绿（退出码 0）。
- 核心计数口径 168/57/370/69/38/66/37/70/88/12/9 全部复核属实。
- 历史 R14 三【中】项（治理面计数、ADM→CS 指令、检索权重双套）经本轮复核均已闭环。
- 三信号/四链路检索权重分属不同层级、各自归一，无事实矛盾。
- 零版本标记纪律、术语单义、数字可实证等元规则经门禁与抽查达标。
